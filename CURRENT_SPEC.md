<!-- Notionミラー元: https://app.notion.com/p/399a8a5dfd45813cacb5dcea4b129871 (📐 CURRENT_SPEC v0.4（正本）) -->

> ## ⚠️ 2026-07-20 訂正（本文より優先）
>
> **「14日目＝偽りの結末」は廃止。** エンディングを14日→28日へ移した際の残骸であり、作者に書く意思はない。本文中の「偽りの結末」記述はすべて無効とする。
>
> **後半の骨組み（確定）**
> - **8日目**：近所の人から「この教会で結婚式を挙げたい」と依頼が来る
> - **8〜14日目**：コマンド「家事」を「**式の準備**」に差し替える（新システムは作らない。ラベルとテキストプールの差し替えのみ）
> - **14日目**：**小さな結婚式**（他人の式を準備して見届ける）。満月。新郎新婦は画面に出さず、CGは「人が帰ったあとの、二人だけの礼拝堂」
> - **17日目頃**：逝去の予兆イベント
> - **21日目**：**葬儀**。天使様が長く関わってきた近所の人（＝野菜をくれていた人）が逝去。天使様は不老不死＝常に失う側。主人公のケアでは治せない唯一の傷
> - **22〜27日目**：大きな外部イベントは置かず、二人の関係に集中
> - **28日目**：追加の**異動指令**。ただし選択権は主人公にある。パラメータに応じてエンディング → **フリープレイ（日付が消える）**
>
> 詳細と理由は DECISION_LOG「2026-07-20｜後半（15〜28日）の骨組みを確定」を参照。

**プロジェクト名（仮）:** 天使様と辺境教会
**文書の目的:** 現時点の仕様を固定し、Claude Code / Cursor への実装依頼をしやすくする
**最終更新:** 2026-07-20（v0.4 改訂：後半骨組・エンディング・痛み5軸・造形確定を反映）
**販売形態:** R-18版のみ・DLsite先行・Steam対象外

# この文書の使い方

- **仕様の正本はこのページ**とする。実装依頼時は「CURRENT_SPEC に従って」と添える
- リポジトリ内の `docs/CURRENT_SPEC.md` は作業用コピー。このページの更新を反映してから実装する
- 仕様変更は**先にこのページを更新**してから実装する
- 判断理由・却下案は `docs/DECISION_LOG.md`、現在地と未確定は `docs/SESSION_STATE.md`、夜ケアの詳細は「🌙 夜ケアパート仕様」を参照

# v0.4 での主な変更点（v0.3 からの破壊的変更を含む）

| 変更 | 内容 |
| --- | --- |
| **販売方針の転換** | **R-18版のみを制作**。全年齢版は当面作らない。DLsite先行、Steamは対象外。v0.3 の「全年齢基盤を先に作る」を撤回 |
| **日数構成の確定** | **28日を本編とする**。v0.3 の「14日MVP＋28日は拡張候補」を撤回。※当初「14日目＝偽りの結末」としていたが、**2026-07-20 に廃止**（14日目は小さな結婚式の日） |
| **夜ケアパートの実装** | ゲーム性を持つミニゲームとして確定（動詞＝「持ちこたえる」）。詳細は別ページ |
| **キャラクター記号の確定** | 糸目／隈／背中の切れ目／宗教モチーフ不使用／長髪。表情システムを規定 |
| **「今はやらないこと」の改訂** | 「成人向け具体化」「本番素材外注実施」を除外。現在まさに進行中の項目のため |
| 維持 | ※**痛みタイプは 2026-07-20 に「6分類→**5軸**・主体＝プレイヤー」に改訂された**。懺悔室・ライティング規範・コマンド4種・アンチパターン群は**すべて維持** |

# ゲーム概要

## ジャンル・販売想定

- DLsite 向け **R-18 短編同棲生活シミュレーション**
- **28日構成**（前半＝天使様を回復させる→後半＝必要はないがそれでもケアする→**28日目にエンディング**→フリープレイ）
- **R-18版のみを制作**。全年齢版は当面作らない
- Steam は成人向けの決済リスクが高いため対象外
- 全年齢/R-18 差し替えアーキテクチャは実装済みだが、**当面 r18 モード固定で運用**する（将来の可逆性のために機構のみ維持）

## 世界観・設定

| 項目 | 内容 |
| --- | --- |
| 主人公 | 大教会から辺境教会へ派遣された世話役。栄転ではなくほぼ左遷 |
| 舞台 | **夏の終わり**の辺境の小さな木造教会。争いも大事件もない穏やかな土地。司祭は常駐せず、大教会から巡回が来るのみ |
| ヒロイン | 天使様（表向きはシスター）。大昔からこの土地を見守り、日課として祈りで世界の均衡を保っている |
| 同棲 | 狭い教会のため、主人公と天使様は同じ個室を共有する |
| **二人だけの契約** | **教会本部は、夜の関係を知らない**。これは二人だけのものである |

## テーマ

> **昼のチルさと、夜のギャップが商品の本体**

- **昼**：生活、家事、祈り、休息。天使様は「あなたは休んでいい」側
- **夜**：天使様の側が求めてくる。それによって天使様は回復する
- **主人公は「必要とされること」で、初めて「ここにいていい」を得る**

中心体験は「課題解決（Doing）」ではなく「共在（Being）」。治すのではなく、ここにいていい・休んでいいと感じられる空気を生活の反復で作る。ただし**夜においては、支えられる側と支える側が反転する**。この反転が主人公の居場所を成立させる。

## トーン（テキスト）

- 日本語、静か、優しい。説教臭くしない。カウンセリングっぽくしすぎない
- 天使様は**事務的だが慈悲深い**。主人公の痛みを**断定しない**
- 「ここにいていい」感覚を、少しだけ出す
- 組織原則：**「私があなたを癒します」ではなく「今日は、ここで休んでいい」**

# キャラクター記号（確定）

- **シスター服ベース。既存の宗教モチーフ（†・✡等）は一切使わない**
- **糸目**。基本は糸目、感情や状況に応じて**切れ長**。さらに**妖艶**（終盤中心の特別プリセット）
	- ★**2026-07-21 呼称変更**：旧「半目」→**切れ長** ／ 旧「妖艶半目」→**妖艶**。作者が好むのは目の開度ではなく**目の形（切れ長）**である。発注時は「半目」と言わず「**切れ長・目尻を効かせて**」と伝える（「半目」は眠そうな半開きと誤解される）
- **目の隈**。物語の進行とケアの結果で増減する
- **背中の衣服に横一線の切れ目**。通常は意匠に見えるが、**夕方の祈りの時にそこから翼が出る**。冒頭のハプニングで判明する
- **翼**：白・**付け根が黒**（性の呪いを孿すことの暗喩。普段は不可視で、夕方の祈りで顕現する。立ち絵には描かない）
- 髪は長め（梳くシチュエーション、背を隠す／かき上げる演出のため）
- **ロップイヤー風の毛束（垂れ耳）**：うさぎモチーフを**意匠メイン**で入れる。髪の一部として付ける（ヴェールを取っても残る）。感情で動く：平常＝垂れる／喜び＝軸が上がる／不安＝深く垂れて先が内側に巻く。疲労が高いと重く垂れる（隈と同じ導出値）
	- ★**発注要件**：垂れ耳の毛束を**別の可動レイヤー**として分離すること。**耳の内側にピンクを入れない**（ピンクは目に集中させる）
- **ヴェール**：黒。終生誓願・未亡人の意味は**不採用**。**「背中の切れ目を隠す実用の布」**として存在する（取ると切れ目が見える＝昼夜のギャップ）
- **配色（確定）**：服＝**黒** ／ ヴェール＝**黒** ／ 髪＝**明るめブラウン** ／ 垂れ耳の毛先＝**黒（はっきり分ける。グラデーションにしない）** ／ 翼＝**白＋付け根黒** ／ 差し色＝**金（腰のチャーム）**
	- 黒の統一構文：**服・翼の付け根・毛先** はすべて「聖性の裏側・本性が滋む場所」
- **目の色**：通常＝ブラウン／**妖艶時＝桃色**。色気ではなく**本性の露見**。夜の後半で使う（うさぎの赤い目とも接続）
- **腰の金のチャーム ＝ 夕方の祈りの魔法陣**（同じ意匠。素材一つで両用）。**月のモチーフ**。28日は月の周期に近い（14日目＝満月）
	- ⚠️ **三日月単体を紋章にしない**（イスラムの星月章として読まれる）。**三相の月も避ける**（ウィッカ）。満ち欠けの連続・円環、うさぎとの組み合わせで回避する
- ★**教会（天使様）のシンボルマーク ＝ 青い炎環**（2026-07-29 採用）
	- **夕方の祈りの演出**：周囲に置いたロウソクの火が繋がって**一つの輪**になり、その中央で天使様が祈る
	- 根拠：炎の**外炎（青）＝完全燃焼・最高温**。無重力では炎は**青い球**になり、すすが出ず**ゆらがない**（NASA）＝「地上の火とは別物の聖なる火」
	- ★**環＝終わりがない＝永遠**。裏エンドで受け取る「教会のシンボルのネックレス」は輪の形であり、**それを受け取った者が永遠になる**ことと形が一致する
	- ★**ロウソクが繋がって一つの輪になる＝移し火**。個々のロウソク（燃料）は燃え尽きるが、輪（火）は残る。**天使様と村人の関係そのもの**であり、21日目の葬儀と直結する
	- これにより三日月の紋章化リスクを完全に回避できる
	- ⚠️ **未確定**：月チャームとの関係（炎環が魔法陣の座を取るのか両立するのか）／差し色の金に青が加わる件。**提案：青は祈りの時だけ現れる色にする**（昼＝黒と金／夕方＝青／夜＝桃色 の三相）。月は空・窓の風景として残す
- 独自宗教のアイコン（魔法陣等）は使用可。実在宗教モチーフは不使用

## 表情システム

**立ち絵1ポーズ = 11レイヤー**（レイヤー分離／PSD納品で合成し、組み合わせ爆発を避ける）

1. **感情セット**（口／眉／頬／涙）：笑顔・大笑顔・怒り（頬膨らませ＝本気の怒りではない）・悲しみ・驚き ＋ 平常
2. **目**：3枚（**糸目 ／ 切れ長 ／ 妖艶**）。★**段階はレイヤーを増やさず「色」で作る**（ブラウン→桃色）
3. **隈**：2枚（薄／濃。**αクロスフェードで無段階に見せる**）。**angelFatigue からの導出値**とする（専用の状態変数は持たない）

＋ **ロップイヤー風毛束の可動レイヤー**（上記とは別に分離発注すること）

- **妖艶は特別プリセット限定・終盤中心・全28日で3〜5回**。判断基準＝**「天使様が主人公を必要としていることが漏れる瞬間」**（時間帯ではなく意味で縛る）
- ⚠️ **開眼は廃止**（2026-07-21）。**夜のCGでも立ち絵と同じ目（切れ長／妖艶）を使う**。カッと見開く表情は作らない
	- 理由：作者の好み。「目を開かせない」ことではなく**切れ長の目そのものが好き**であり、見開くとその魅力が消える
	- 昼夜のギャップは「開眼の有無」ではなく、**目の色（ブラウン→桃色）＋吐息・汗・頬の赤み**で担保する
	- ※ 旧方針（D-14 / D-18）の「開眼は夜の1枚絵でだけ見せる」は**廃止**

### 隈の結線（U-02 確定）

- **kumaLevel は状態変数として持たず、angelFatigue から導出する**
- **長期の変化は「日中に溜まる疲労量そのものが減っていく」で表現**する。物語が進む＝天使様が主人公を頼れるようになる＝**疲労蓄積レートが下がる** → angelFatigue の平常値が下がる → 隈が薄くなる
- **判定タイミングを昼夜で分ける**
	- **昼の立ち絵**：その日の**朝の angelFatigue でスナップショット固定**（日中はガタつかない）
	- **夜のCG**：**リアルタイムの angelFatigue**（その夜の状態が出る）
- **例外：オープニングはシステム外**。OPの絵は導出値を使わず、演出として直接指定してよい

> 昼＝立ち絵、夜＝1枚絵という**メディアの切り替えそのもの**が、作品の核である昼夜のギャップを担保する。目は昼夜とも同じ3枚（糸目／切れ長／妖艶）を使い、**色と付随する表現**でギャップを作る。

# 時間帯

- **夕方＝天使様のお仕事（祈り）の時間**。翼が出るのはこの時間帯
- **正体判明も夕方**

# 日数構成（v0.4 確定：28日本編）

## 設計要件

1. **日数非依存のデータ構造**：イベントは day 直指定だけでなく phase でも紐づけられる
2. **28日目＝エンディング**：追加の異動指令が来るが、**選択権は主人公にある**。**エンディング後も日付は続く（上限99日）**。裏エンドで**日付が消え、フリープレイが解放される**
3. **通常日と重要日の分離**：重要日（固定イベント日）と通常日（自由行動＋短文プール）を構造的に分ける
4. **「何も起きない日」の許容**：役に立たない日があっても翌日に世界が変わらず在ることが体験の核

## フェーズ構成

| フェーズ | 期間 | 焦点 |
| --- | --- | --- |
| 境界と探り合い | 1〜3日目 | 到着、夕方の祈りでの正体開示（翼）、生活の学習。会話で painPoints 蓄積開始（3日目に食事イベント）。※**3日目での painType 確定は廃止** |
| 受容の定着 | 4〜7日目 | 「正体を知っても変わらない朝」。懺悔室の解禁（5〜7日目） |
| 安全基地と開示 | 8〜11日目 | **8日目：結婚式の依頼**が舞い込む。以降14日目までコマンド「家事」→「式の準備」に差し替え。覚えていてくれる回収 |
| **小さな結婚式** | 12〜14日目 | 式の準備が続く。**14日目＝他人の結婚式**（満月）。新郎新婦は画面に出さない。CGは「人が帰ったあとの、二人だけの礼拝堂」。ここで primaryPain が1回だけ入れ替わりうる |
| 動機の転換 | 15〜18日目 | angelFatigue は概ね取れている。「必要はないが、それでもケアする」へ。**bond** が主役になる |
| 失う側の天使様 | 19〜23日目 | **17日目頃：逸去の予兆**。**21日目：葬儀**（野菜をくれていた近所の人が逸去）。天使様は不老不死＝**常に失う側**。主人公のケアでは治せない唯一の傷 |
| 二人に集中し、選ぶ | 24〜28日目 | 22〜27日は大きな外部イベントを置かない。**28日目：追加の異動指令**→**エンディング4択**→残る系なら日付は続く（上限99日）→**裏エンド**→**日付が消える＋フリープレイ解放** |

# 夜ケアパート（v0.4 新規）

> **詳細仕様は別ページ「🌙 夜ケアパート仕様」を正本とする。** ここでは全体仕様との接続のみ記す。

- コアの動詞は **「持ちこたえる」**（抗うではない）。天使様に応え続けるために耐える
- **耐えた量 ＝ angelFatigue の回復量**。結果は**翌日の余白と天使様の疲労度に反映**される
- ★**コア構造（2026-07-22 確定）**：昼の**心身の余白**の割合から、夜の**精力**を算出する。夜は**基本オートで進行**し、プレイヤーが起こせる能動は**残った余白を精力へ追加変換すること**だけ。変換すれば**2発目**が可能になり、夜を長く続けられる
	- 眺めるのが主。能動は「もう一発いくか」の一点のみ（連打ではない）ので、ASMR的な没入を殺さない
	- 昼のトレードオフが生きる：余白を昼で使う（会話・ケア・痛みの回収）か、温存して夜を伸ばすか
	- フリープレイでは変換の上限が外れ、実質無限になる（プレイヤーが「今夜は終わり」で終了）
- 失敗は「負け」ではなく「**天使様の回復が浅い**」という結果になる。ゲームオーバーなし
- **ポジション4つ**。進行で置き換わるのではなく**選択肢として累積**し、以降いつでも切替可能
- ★**夜は回避できない**（2026-07-27）。おあずけは選べるが、**3日経つと天使様側の我慢が効かなくなる**（設定上、止められない）。プレイヤーがどれだけ拒んでも、**天使様は必要とし続ける**
- ⚠️ **例外：7日目と21日目は夜なし**（2026-07-29）。7日目は告白の夜（**抱き編められて眠るだけ。エロなし**）、21日目は葬儀。逆に**14日目（小さな結婚式）は強制の夜**
	- 7日目は **angelFatigue が回復しない**（天使様は我慢した）が、**trust は上がる（+5）**。告白して受け止められる日であるため
	- ★**強制の夜なしは、おあずけの連続カウントに算入しない**（カウンタ据え置き）
- ★**おあずけの解禁条件**：**信頼が閾値50以上 ＋ 8日目以降**。前半の主人公は「断れない」――**それ自体が症状**であり、解禁は**症状が軽くなった証**。床を8日目に置くのは、7日目に天使様が「嫌なことには嫌だと感じた、その心に従って良いのですよ」と言うから
- ★**疲労の回復量は昼の余白から導出される**（2026-07-29 確定）。**段階5・レンジ 2〜6**（余白 0-3→-2 / 4-7→-3 / 8-11→-4 / 12-15→-5 / 16-20→-6）
	- 因果：**休む人は余白が多い → 長く耐えられる → 天使様がよく回復する**。「昼に怠けて、夜に捧げる」
- **全28夜がケア対象**。ただし**「おあずけ」択**があり、天使様は咎めない
- 絵は**レイヤー合成方式**（2026-07-21 確定）：ベース4枚（ポジション別）の上に、**目／口／汗・湯気**を独立レイヤーで重ねる。各パーツ数枚ずつで、組み合わせにより多彩な表情が出る
	- **口は輪郭が動く**ため、単純なレイヤー重ねでは破綻する。対策は **B＋C**：
		- **B（普段）**：開口の段階を**輪郭が動かない範囲**に絞る（引き結ぶ／薄く開く／息が漏れる）。切れ長の目の美学と一致し、素材も軽い
		- **C（ここぞ）**：**Z（天使のクライマックス）の瞬間だけ**、輪郭ごと描いた専用の顔を1枚差し込む。口が大きく開き、妖艶＋桃色になる。**全ての抑制が一点で決壊する1枚**
	- 湯気は**汎用エフェクトレイヤー**を流用（食卓／浴室／夜CGで共用。露出コントロールにも使える）

## ⚠️ アンチパターンとの接続（厳守）

- 夜ケアで回復するのは **angelFatigue（天使様の疲労）**であって、**主人公の painType ではない**。この2つを絶対に混ぜない
- 「痛みの数値ゲージ化とゼロ到達＝クリア」は**引き続き禁止**
- **懺悔室の性的演出への転用は引き続き禁止**

# 痛み（painPoints・5軸）※2026-07-20 改訂

> ★**痛みの主体は【プレイヤー】**である（主人公ではない）。主人公は**投影の器**として振る舞う。
> 旧「6分類」は **5軸** に改訂。`loss` は主軸から外し、**作品全体の通奏低音**として背景に置く（21日目の葬儀、天使様の不老不死などで扱う）。

| ID | 名前 | 内面 |
| --- | --- | --- |
| useless | 役に立てなかった痛み | 期待に応えられなかった無力感 |
| not_needed | 必要とされたい痛み | 代替可能な歯車として扱われた孤独 |
| no_rest | 休めない痛み | 何かしていないと不安。休む罪悪感 |
| no_place | 居場所がない痛み | どこでも部外者。左遷による拒絶感 |
| self_blame | 自分を責める痛み | すべて自分のせいと捉える自己処罰 |
| ~~loss~~（背景化） | 喪失や別れの痛み | ⚠️ **5軸から除外**。painPoints の軸としては持たず、作品全体の通奏低音として背景に置く |

## ★ useless と not_needed の分離（2026-07-27）

両者は似ているが構造が違う。**選択肢と天使様の返しを必ず書き分けること。**

|  | useless（役立たず） | not_needed（必要とされない） |
| --- | --- | --- |
| 何の話か | **行為の評価**。「価値を生めていない」 | **関係の不在**。「誰も求めていない」 |
| 主語 | 自分（能力・成果） | 他者 |
| 解消の方向 | **証明**すれば楽になる（と本人は思う） | 証明では埋まらない。**誰かが求める**しかない |
| 告白の言葉 | 「役立たずなんです」＝**自分の能力を語る** | 「誰も気にしてないんです」＝**他者の不在を語る** |
| 天使様の返し | 「できなくていい」 | 「わたくしが必要としています」 |

★**両者は誤変換される**：「必要とされたい」を「役に立てば必要とされる」に置き換えてしまう。しかしこの変換は失敗する（役に立って得られるのは**条件付きの取引**であり、役に立たなくなった瞬間に消えるため）。

★**統計的裏付け**：日本では「他者にとって自分が役に立つ存在かどうか」が自己肯定感に大きく関連しており、**これは諸外国に見られない特有の傾向**（こども家庭庁 令和5年度調査）。→ 参考資料フォルダ「リサーチ：現代人の苦悩の統計」

★**本作の位置づけ**：天使様は設定上、主人公を必要としている。主人公は有能さも成果も要らない。つまり本作は **useless の回路を通さずに not_needed を満たす装置**である。

## 実装ルール（2026-07-20 改訂）

- **不可視蓄積**：会話選択肢に痛みベクトル（painPoints）を仕込む。画面には一切表示しない
- ★**拾うのは「プレイヤーが語ったとき」だけ**。行動から推測しない。語らないプレイヤーには踏み込まない（語らない自由を守る）
- **入口は3つ**（2026-07-21 改訂）：
	1. ★**信頼解禁の会話イベント**（主軸）。信頼が閾値に達すると**天使様の方から問いを投げる**。信頼はケアで自然に上がるため、**受け身のプレイヤーでも自動で入口が開く**
	2. **食事イベント（初回のみ）**。日常の導入として残す
	3. **7日目の告白**（補完）
- ★**行動の偏りからの回収も併用する**（完全受動の入口）。rest多用→「最近、よく眠れるようになりましたね」等。プレイヤーが一言も語らなくても行動そのものから拾える
- 答えは **memoryFlags** に記録し、後の会話・ピロトーク・エンディングで返す。**深い反映は2〜3個に絞る**（残りは「覚えている素振り」だけ見せる）
- **`primaryPain`**：確定値ではなく**導出値**。**14日目に1回だけ入れ替わる**余地を持つ
- **状態管理**：painSeeds は数値ゲージではなく状態遷移：**hidden → touched → shared**。**ゼロにする（完治させる）ことをクリア条件にしない**
- **参照先**：夜ケア台詞・後日回収台詞・エンディングのモノローグ
- **診断禁止**：病名・診断・治療の語彙は使わない
- ⚠️ **旧仕様の廃止**：「3日目夜終了時に painType を確定」は**廃止**。人の痛みは1つではなく、また3日縛りは7日モックの都合（END_DAY=7）を引きずっていたため

# 懺悔室（傾聴の場）（維持）

## 設定（カトリック考証準拠）

- 司祭不在の辺境教会には、昔使われていた**古い木製の懺悔室**が残っている
- 正式な告解（ゆるしの秘跡）は司祭にしかできないため行わない。天使様はこの空間を**「傾聴の場」（霊的同伴）**として再利用している
- **天使様は絶対に「赦します」と宣言しない**。受け止める・祈る・共にいる、のみ
- 木製の格子越しで互いの表情は見えない。この視覚遮断が自己開示のハードルを下げる

## イベント仕様

- **解禁**：5〜7日目の夜、天使様からの招待イベントで解禁
- **選択肢は常に3つ**：「少しだけ話す」「ごまかす」「黙る」
- **すべて等価に肯定**：どれを選んでも好感度ペナルティなし。沈黙には「無理に言葉を探さなくていいのですよ」等の肯定を返す
- **記録**：confessionChoices に選択履歴を残し、後日の夜ケアで天使様が肯定的に言及する
- **攻略情報化の禁止**：正解探しにさせない
- 28日構成では懺悔室イベントは**最低3回**（解禁時／11日目前後／残留後の再定義）を想定
- 専用画面は作らない。1画面UIを維持し、CSSでトーンを落とし格子のシルエットを表現

> **懺悔室は性的演出に転用しない。** 夜ケアパートとは完全に別系統の場として維持する。

# エンディング（2026-07-20 確定）

28日目に追加の**異動指令**が来るが、**選択権は主人公にある**。

> ★**2026-07-27 改訂：構成を「達成」から「選択」に変えた。**
> 旧構成では帰任がゲートなしのフォールバックだったため、結果として「**何も達成しなかった人が行く場所**」＝実質的なバッドエンドになっていた。さらに祈り／世話役は“役に立った”の証明書であり、**作品が最も否定したい useless の回路**でエンディングが分岐していた。
> → **無条件で選べる2つ**（役割なく残る／帰任する）＋**条件を満たすと増える2つ**（祈り／世話役）に変更。**何もしなかったプレイヤーにも行き場があり、積み上げた人は選択肢が増える**。バッドが消える。

| エンディング | 到達条件 |
| --- | --- |
| **役割なく残る**（新設） | **無条件**。常に選べる。天使様は引き止めの言葉を伝えるが、**最後の一言を言うのは主人公**（自分から「残る」と言う）。**28日間ずっと必要とされてきたから、最後に一度だけ自分から言える** |
| **帰任する** | **無条件**。常に選べる。自分の意志で帰る（“届かなかったから”ではない） |
| **祈りエンド**（残留） | `祈りの調律` が閾値以上 ＋ **疲労ゲート** |
| **世話役エンド**（残留） | `世話役適性` が閾値以上 ＋ **疲労ゲート** |

- **疲労ゲートは「残る系」の2つにのみ課す**。全エンドに課すと、疲労が高いまま28日目を迎えたプレイヤーがどこにも行けなくなる（詰みの発生）
- ⚠️ **`信頼`** **は判定材料から除外**。前半の推進役・疲労軽減の補助としては存続する
- ⚠️ **`bond`** **も判定材料にしない**。完全非公開の値をエンディングの鍵にすると、「何をすれば良いか分からないまま隠しパラメータで裁かれる」事態になる
- **「様子を見る」で、祈りの調律／世話役適性の傾向をそれとなく示唄する**（数値は見せない）。bond（完全非公開）とは**隠し方の強度を意図的に分ける**

## 帰還を選んだ場合の内部分岐

メタ的な表現（システムメッセージ等）は使わず、**天使様自身の言葉で語らせる**。

```
疲労低：帰還を選択 → 天使様が記憶を消すことを語る →「それでも帰る？」
　　　　　　┣ はい → なにもなかったエンド
　　　　　　┗ いいえ → 祈り／世話役に戻る
疲労高：帰還を選択 → より素っ気ない言葉 → そのままなにもなかったエンド
　　　　　　（引き止める選択肢自体がない）
```

**なにもなかったエンド**：主人公は28日間の記憶をすべて失い、左遷前まで時間が戻る。左遷の話自体がなくなり、別の人生を歩む。**天使様だけがすべてを覚えている**。

- ★執筆注意：疲労低と疲労高のセリフは**口調の温度差で書き分ける**（低＝静かで痛みを伴う短さ／高＝投げやりで冷たい短さ）。**分岐の長さの非対称性そのもの**が、それまでのプレイの結果を語る
- 却下案：疲労高での残留（脅迫による束縛になり、テーマに逆行）／セーブデータ削除（既存設計と不整合・商業リスク）

# 夜の記録（nightRecords）（2026-07-20 新規）

R-18要素の回数表示。**完全公開情報**として持ち、「様子を見る」夜画面で表示する。

- **`???`** ＝未解放（まだ二人が知らないこと）／**`0回`** ＝解放済みだが未発生
- ★**unlocked フラグで管理する**。「0なら???」にすると、初夜から解放済みだが0のまま推移しうる項目で破綻する
- 項目（記号管理。具体の内容は実装時に作者が定義）：**KI / KO / SE / TA**（順次解放）＋ **S / Z**（初夜から解放）
- **拡張可能な定義データ構造**で持つ（項目追加が1行で済む形）。夜ケアのミニゲーム設計を詰めると項目が増える見込み
- 解放条件：「**夜ケアの累積成果（careStage 到達度 or 成功回数）＋日数下限**」で**仮置き**。ミニゲーム確定時に見直す
- ⚠️ 「心身の余白（上限）を解放条件にする」案は**却下**（上限値は現状変動しない・役割が濁る・「ケアの成果」ではなく「主人公の余裕」であるため）

# 裏エンディング（2026-07-20 新規）

**エンディング後の「おまけ」**として扱う。**上位互換の真ENDではない**。

- **位置づけ（2026-07-29 改訂）**：裏エンドは**28日目のエンディングの後**に来る。フリープレイは裏エンドの**後**に解放される
- 28日目に残る系を選ぶと、**日付は表示され続ける**（上限**99日**）。関係が深まった先で裏エンドに入る
- **解放条件（OR）**：**29日目以降に貯めた nightRecords が閾値到達** または **99日経過**
	- 本編28日間の数値は算入しないため、**本編中に夜を稼ぐ動機が一切生まれない**（「性は痛みを治さない」原則を守れる）
	- OR条件により**どんなプレイでも必ず到達する**（詰みの防止）。**99日到達時は強制発生**
	- おあずけは3日で切れる設計のため、実際は大半が nightRecords 側で先に到達する。99日は作動しない安全弁
	- ★**ネックレスを受け取った時点で日付表示が消える**。旧設計では「日付が消える」はフリープレイのUI都合だったが、**永遠になった瞬間に時間の表示が失われる**という物語上の因果になった
	- 却下案：フリープレイの日数を条件にする（**フリープレイは日付が消える**ため、条件が完全に不可視になる）
- **内容：純愛エンド**。主人公は天使様から**教会のシンボルのネックレス**を贈られ、**永遠の存在になる**。**天使様はついに失わない**
	- 全企画共通の「**常に身に着けるものを贈る**」文化に依拠する（→ 00_shared「共有世界設定」）
	- **システム的に説明しない**。当人たちも周囲も、それに気づかない
	- **幸福かどうかは描かない**。プレイヤーの想像に委ねる
	- 伏線：**21日目の葬儀**で「死者はかつて贈られたモノとともに埋められる」を見せておく。ネックレスだけが**永遠になるがゆえに、決して埋められない**
	- 却下案：快楽堕ち・堕天エンド（羽が完全に黒くなる）― 設計上は成立するが、飽和したトロープを書き切る技量が現時点でないと作者が判断
- ⚠️ **以下2行は旧設計**（フリープレイ突入後に裏エンドが解放される前提）**。2026-07-29 の改訂により無効。**
- **見せ方**：閾値到達→その夜の終わりに控えめな示唄→以降**タイトル画面かエンディング回想から選択可能**になる（フリープレイ中に突然始まらない）
- フリープレイ突入直後に**ミニ会話**を入れ、存在を示唄する（攻略情報として明示しない）

# ポストゲーム構成（2026-07-29 新規）

裏エンド後は**二層構成**になる。

## ① フリープレイ（本編の続き）

- 裏エンド後、**本編セーブから日付表示が消える**。そのままプレイを継続できる
- **昼パートあり**。余白→精力の変換システムもそのまま生きる。蓄積も継続する
- **文脈のある永遠**：あの二人の生活が続いている状態

## ② フリーモード（タイトル画面から解放）

- 本編とは別に、タイトルから直接入れるモード
- **夜パートをノーコストで遊べる**（リソース制限なし・無制限）
- **文脈のないアクセス**：見たいものをすぐ見るための場所
- **フリーモードのプレイは nightRecords に算入しない**（推奨）。nightRecords は「二人の関係の記録」であり、文脈のないサンドボックスの数値が混ざると意味が濁る

★**役割を重ねないことが重要**。フリーモードが夜を無制限に提供する以上、フリープレイの価値は「**昼を含む生活が続くこと**」に置く。

## ★ なぜ本編が終わってはいけないか

裏エンドは「主人公が永遠の存在になる」話である。**ここでゲームが終わると、その結末が嘘になる。** 遊びが終わらないこと自体が、永遠の実演になっている。したがって裏エンド後にプレイが続くのは演出上の必然であり、単なるおまけではない。

## その他

- **継続期間に季節変化を入れない**。素材を増やさないため、かつ**天使様の周りでは時間が止まっている**という主題と一致するため
- **フリーモードの解放条件はタイトル画面か README に明記する**（隠さない）
- ★**28日以降、日付は物語的な意味を失う**。これは欠陥ではなく**時間喪失の二段階目**として機能する（①日付が無意味になる＝永遠の予行演習 → ②日付が消える＝永遠そのもの）

# ライティング規範（維持）

会話は以下の**5ステップテンプレート**を基本形とする（全部を毎回使う必要はない）。

1. **観察とグラウンディング**：即座に慰めず、状態や環境（風の音、お茶の湯気）に意識を向けさせる
2. **感情の反映**：言葉を柔らかく言い換えて受け止める
3. **非評価**：痛みの背景を解きほぐす。「誰でもそうです」と矮小化しない
4. **小さな許可**：「〜すべき」ではなく「〜してもよいのです」の形
5. **記憶と連続性**：その場で解決せず、覚えておくことを伝える

**NG表現**：「頑張ればきっと大丈夫」「考えすぎです」「みんな同じように苦しんでいます」「私があなたを救ってあげます」「時間が解決してくれます」。現代心理学用語（トラウマ・ストレス等）を台詞に出さない。天使様による赦しの宣言・診断・治療風の解決も禁止。

**天使様の口調**：柔らかい丁寧語（〜ですね / 〜ですよ）。断定と説教を避け、「私には〜のように見えます」の I-message。祈りや教義を盾にしない。

# 設計上のアンチパターン（禁止事項・維持）

- 天使様の万能救済者化（透視・神託・全解決）
- **痛みの数値ゲージ化とゼロ到達＝クリア**
- 正解選択肢で心を治すゲーム化（間違いでバッドエンド）
- 依存・独占の過度な美化
- 最終日の急な使命・劇的展開（魔物襲来・追手など）
- **懺悔室の攻略情報化・性的演出への転用**

# コマンド（維持）

自由行動フェーズ（2日目朝〜）でのみ表示。少数・固定4種：家事（chores）/ 祈る（pray）/ 休む（rest）/ 話しかける（talk）。行動回数の偏りを記録し、後日の天使様の言及に使う。

# 今はやらないこと（v0.4 改訂）

- 全年齢版の制作（必要になったら改めて判断。技術baseは維持）
- Steam展開
- Live2D
- 複数ヒロイン
- 大規模分岐
- 多言語
- サーバーセーブ
- ボイス（夜のみボイスは将来検討。センシティブ扱い・git非管理）
- BGM / SE（将来）

> v0.3 にあった「成人向け具体化」「本番素材外注実施」は、**現在まさに進行中のため除外**した。

# 技術（現状）

- HTMLモック：企画検証用。外部ライブラリなし、データ駆動。1〜7日目まで通しプレイ可能
- 全年齢/R-18 差し替え基盤：実装済み（`content-config.js` / `asset-resolver.js`）。当面 r18 固定
- 夜ケアCSVパイプライン：実装済み（`night-care.csv` → `tools/build-nightcare.js` → `night-care-data.js`）
- gitガードレール：adult/r18 素材、`**/*_adult.*`、制作元データを先行除外済み
- 本実装：Unity側

# 変更履歴

| 日付 | 内容 |
| --- | --- |
| 2026-07-01 | 初版作成（モック v0.2 実装に基づく MVP 仕様固定） |
| 2026-07-10 | v0.3: 日数構成（14日MVP＋28日対応設計）、痛みタイプ6分類、懺悔室、ライティング規範追加、管理正本を Notion に移行 |
| 2026-07-13 | **v0.4: 販売方針をR-18のみに転換。28日を本編として確定（14日MVPを撤回）。夜ケアパートのゲーム性を組込み。キャラクター記号・表情システムを明文化。「今はやらないこと」を改訂** |
| 2026-07-20 | **v0.4 改訂：「14日目＝偽りの結末」を廃止し、後半の骨組（結婚式・葬儀・28日目の異動指令）を確定。エンディング3種＋帰還内部分岐を確定（信頼を判定材料から除外）。痛みタイプを 6分類→5軸・主体＝プレイヤーに改訂（３日確定を廃止）。造形を確定（翼の黒は付け根・普段不可視／配色／ロップイヤー／ヴェールの意味／月チャーム）。表情システムを 3レイヤー→11レイヤーに。nightRecords と裏エンディングを新規追加** |
| 2026-07-29 | **v0.4 改訂：目の呼称を変更（半目→切れ長、妖艶半目→妖艶）し、**開眼を廃止**。夜CGをレイヤー合成方式（目／口／汗・湯気）＋Zの決壊1枚に。夜ケアのコアを「余白→精力変換で2発目」で確定。夜は回避不可（おあずけは3日で切れる）。useless と not_needed を分離。痛みの入口を信頼解禁の会話イベント主軸に。**エンディングを4種にし、「達成」から「選択」の構成へ変更**（バッドの解消）。裏エンドをエンディングの後に移し、99日上限とOR条件を導入。ポストゲームを二層構成（フリープレイ／フリーモード）に。シンボルマークを**青い炎環**に決定** |

---

## 実装補足（ローカル固有・Notion未収録）

> 以下は Notion 版 CURRENT_SPEC v0.4（上記本体）には無い、**現行 HTML モック（v0.2〜v0.3 実装、1〜7日目・14日/全年齢前提の記述を含む）の実装仕様**。Notion v0.4 は 28日・R-18本編の設計であり、現行モックの実装詳細（画面構成・データ構造・ファイル一覧等）はそちらでは扱われていないため、情報を失わないようここに保全する。内容は旧版（2026-07-03 最終更新）のまま、逐語で保持する。日数・販売方針などの記述が本体（Notion v0.4）と食い違う場合は **本体（Notion v0.4）を優先**する。

**文書の目的（旧版）:** 現時点の MVP 仕様を固定し、Cursor / Claude / Codex への実装依頼をしやすくする
**最終更新（旧版）:** 2026-07-03（夕方スロット・7日目モック・夜ケアデータ分離を反映）
**対応実装:** 企画検証モック v0.2〜v0.3（`index.html` / `style.css` / `scenario-data.js` / `night-care-data.js` / `app.js`）

### この文書の使い方（旧版）

- **実装の正本はこのファイル**とする。AI に依頼するときは「CURRENT_SPEC.md に従って」と添える。
- **体験の核・将来方針** は `CONCEPT.md` を参照する。
- 仕様変更は **先にこのファイルを更新** してから実装する。
- 今回の文書は **仕様整理のみ**。コード変更は含まない。

### ゲーム概要（旧版・現行モックの前提）

#### ジャンル・販売想定

- DLsite 向け **短編同棲生活シミュレーション**（全年齢基盤を先に作る）
- 最終版イメージ: 当初 **14 日間**の短期任務。癒し・生活への馴染みのため **28 日構成の可能性** あり（§日数構成の検討メモ。**正式決定ではない**）
- 現モック: **1 日目（固定オープニング）＋ 2〜7 日目（自由行動）** まで遊べる（`END_DAY = 7`）

#### 世界観・設定（旧版）

| 項目   | 内容                                                            |
| ---- | ------------------------------------------------------------- |
| 主人公  | 大教会から辺境教会へ **14 日間の短期任務** で派遣された世話役。栄転ではなく、ほぼ左遷に近い            |
| 舞台   | 辺境の小さな **木造教会**。争いも大事件もない、穏やかな土地                              |
| ヒロイン | **天使様**（表向きはシスター）。大昔からこの土地を見守り、日課として神力・祈りで世界の均衡を保っている         |
| 同棲   | 狭い教会のため、主人公と天使様は **同じ個室** を共有する                               |
| 時間帯  | 夜は日課で疲れている。主人公は就寝前に **ケア / 軽いマッサージ** で疲れを取る。2 日目以降は **毎晩の日課** |

#### テーマ（旧版）

> **天使様を支えているつもりが、主人公自身も天使様に見守られ、受け入れられていく**

#### トーン（テキスト）（旧版）

- 日本語、静か、優しい
- 説教臭くしない。カウンセリングっぽくしすぎない
- 天使様は **事務的だが慈悲深い**
- 主人公の痛みを **断定しない**
- 「ここにいていい」感覚を、少しだけ出す

### MVP 範囲

#### 現モック v0.3 で **やっていること**

| 区分   | 内容                                           |
| ---- | -------------------------------------------- |
| 日数   | **1 日目（固定オープニング）＋ 2〜7 日目（自由行動）**             |
| 時間   | 朝 / 昼 / **夕方** / 夜 の **4 区分**（`morning` / `noon` / `evening` / `night`） |
| ヒロイン | **1 人**（天使様）。正体は **1 日目で判明済み**。2 日目以降は知った上で生活 |
| UI   | **1 画面**完結（タイトル / 本編 / 終了は同一 HTML 内の画面切替）    |
| コマンド | **朝・昼:** 家事 / 祈る / 休む / 話しかける ＋ **様子を見る**（5 ボタン） |
|        | **夕方:** 家事 / 祈る / 休む / 様子を見る（**話しかける不可**・4 ボタン） |
|        | **夜:** 行動選択なし（強制ナイトイベント → 毎晩ケア → 就寝） |
| 分岐   | 1 日目夜の選択、2 日目夜・3 日目夜の強制イベント、**7 日目夜の懺悔室イベント**（4 択） |
| 毎晩ケア | **2 日目以降毎夜**（§毎晩のケア）。本文は `night-care-data.js` |
| セーブ  | `localStorage` で `gameState` 全体を保存           |
| 素材   | **プレースホルダーのみ**（立ち絵枠＋表情テキスト）                  |

#### 1 日目（固定オープニング・シーン連鎖）

線形シーン＋分岐選択。自由行動コマンドは **出さない**。

| 流れ  | 概要                                                        |
| --- | --------------------------------------------------------- |
| 昼   | 辺境教会に到着。シスター（天使様）が出迎え                                     |
| 昼   | 個室へ案内。**屋根裏部屋に入るな** と警告                                   |
| 昼→夜 | 休む → 物音で目覚める                                              |
| 夜   | 屋根裏で **翼・魔法陣** を目撃 → 火を消す → **天使であることが判明**                |
| 夜   | 手料理を振る舞う → 肩揉み等で **初回ケア**（`night_care`）→ 「2 日目の朝へ」で自由行動開始 |

1 日目夜のケアは **毎晩パートの原型**。本編ルーティンとしての毎晩ケアは **2 日目以降**（§毎晩のケア参照）。

1 日目夜の「物音への反応」選択は `memoryFlags` に記録するが、**現 v0.2 では後続 callback 未接続**（保留）。

#### 2 日目

| 時間  | 内容                                       |
| --- | ---------------------------------------- |
| 朝   | 自由行動開始。鳥の声・夏の終わりの空気（環境音 SE は未実装・コメントのみ）  |
| 朝/昼/夕 | 行動いずれか → 日/time/action に応じた短い反応（夕方は §夕方の自由行動） |
| 夜   | **強制イベント**（疲れへの気づき等）→ **毎晩ケア** → 就寝 → 翌朝 |

#### 3 日目

| 時間  | 内容                                                       |
| --- | -------------------------------------------------------- |
| 朝/昼 | 4 コマンドいずれか。2 日目に話していた場合の **短い callback** あり              |
| 夕方 | 夕方コマンド（§夕方の自由行動）。天使様は屋根裏部屋で祈りの務め               |
| 夜   | **強制イベント**（2 日目夜 callback 分岐）→ **毎晩ケア** → 就寝 → 翌朝 |

#### 4〜6 日目（通常日）

固定の強制ナイトイベントなし。朝・昼・夕の自由行動と毎晩ケアで生活が続く。

| 日   | 雰囲気（文案の目安） |
| --- | ------------ |
| 4 日目 | 正体を知った上での穏やかな生活の延長 |
| 5 日目 | 教会生活に慣れ、天使様の不器用さ・地域とのつながりが少し見える |
| 6 日目 | 7 日目に向けた静かな気配（懺悔室・祈り・沈黙。痛みの深掘りはしない） |

各日 **朝・昼** は `slotIntroTexts`（`dayN-morning` / `dayN-noon`）と `freeActionTexts`（`dN_morning` / `dN_noon`）で day 別テキスト。**夕方**は共通 `eveningDefault` にフォールバック（§夕方の自由行動）。

#### 7 日目

| 時間  | 内容 |
| --- | --- |
| 朝/昼 | 通常日。`day7-morning` / `day7-noon` と `d7_morning` / `d7_noon` |
| 夕方 | `day7-evening` / `d7_evening`（懺悔室イベント前の静かな気配） |
| 夜   | **懺悔室イベント**（4 択・`memoryFlags` 記録）→ **毎晩ケア** → 就寝 → **モック終了** |

懺悔室イベントの選択肢（いずれも責めない・好感度ペナルティなし）:

| 選択肢 | `memoryFlags` |
| --- | --- |
| 少しだけ話す | `confession_day_shared` |
| うまく言葉にならない | `confession_day_uncertain` |
| 何も言わずに座っている | `confession_day_silent` |
| 今日はやめておく | `confession_day_avoided` |

#### 現モックの **終了条件**

- **7 日目夜**の **毎晩ケア** の後、「就寝する」で終了画面を表示（`END_DAY = 7`）
- 文言: 「モック v0.2 はここまでです。物語は、まだ続いていきます。」（`index.html` の終了画面）

#### MVP 全体（将来の最小完成形）の目安（旧版）

現モックを土台に、**全年齢で遊べる完成版** を目指す（**28 日構成が有力案**。§日数構成の検討メモ・`CONCEPT.md` 参照）。**現行は 7 日モックの拡張・安定化を優先** し、8 日目以降・結婚式・葬儀・帰還指令は未実装。

**旧案（保留）:** 当初は **14 日版** を最小完成形としていた。

> ※ 上記「全年齢で遊べる完成版」「14日版」は v0.3 時点の記述。**販売方針・日数構成は Notion v0.4（本体）で R-18版のみ・28日本編に確定済み。** ここは現行モック実装が準拠していた旧文脈として保全する。

### 画面構成

1 HTML・1 画面 UI。PC ブラウザ想定（スマホ対応は MVP 外）。

```
┌─────────────────────────────────────────────────────────┐
│ タイトル画面                                             │
│  ・はじめから / つづきから / リセット                     │
│  ・セーブ有無の表示                                       │
├─────────────────────────────────────────────────────────┤
│ ゲーム画面（HUD）                                        │
│  ・日数 / 時間帯（朝・昼・夕方・夜）/ 場所                      │
├──────────┬──────────────────────────────┬───────────────┤
│ 天使様   │ メッセージ欄                  │ ログ          │
│ 立ち絵   │ 選択肢欄（シーン進行用）       │ （時系列）    │
│ プレース │ 行動ボタン（自由行動時のみ）   │               │
│ ホルダー │ セーブ/ロード/リセット        │               │
│ 状態     │ デバッグ（ON/OFF 切替）       │               │
│ 距離感   │                               │               │
└──────────┴──────────────────────────────┴───────────────┘
│ 終了画面 → タイトルへ戻る                                │
└─────────────────────────────────────────────────────────┘
```

#### 表示項目

| 要素     | 説明                                                                                                               |
| ------ | ---------------------------------------------------------------------------------------------------------------- |
| 立ち絵    | 枠＋「天使様」「表情: xxx」テキスト。**画像・Live2D は未実装**                                                                          |
| 天使様の状態 | 短い状況文（例: 「朝の支度をしている」）                                                                                            |
| 距離感    | `relationStage` を段階ラベルで表示                                                                                        |
| メッセージ欄 | シーン本文・行動結果・夜イベント                                                                                                 |
| 選択肢欄   | オープニング分岐、夜イベント、続ける                                                                                               |
| 行動欄    | 自由行動フェーズのみ。「なにをしますか？」＋ 行動ボタン（朝・昼は 5、夕方は 4。§コマンド） |
| ログ欄    | `[N日目 時間帯] 短文` の履歴                                                                                               |
| デバッグ   | `day`, `timeSlot`, `phase`, `relationStage`, `stats`, `memoryFlags`, `seenEventIds`, `nightCareCount`, `seenNightCareIds` 等。ON 時のみ「OPスキップ：2日目朝へ」ボタン（§デバッグ用OPスキップ） |

#### デザイン方針

- 暗すぎず、静かな **木造教会** っぽい雰囲気
- **低彩度**、**余白多め**
- 夜は UI トーンを少し落とす（`.time-night`）
- 画像なしでも「祈り・静けさ・自然」を感じる typography と色

### 日数と時間帯

#### 内部キー

| キー        | 表示  |
| --------- | --- |
| `morning` | 朝   |
| `noon`    | 昼   |
| `evening` | 夕方  |
| `night`   | 夜   |

#### 夕方の位置づけ

- **天使様のお祈り仕事タイム**。屋根裏部屋で日課の祈りの務めに入る
- この時間帯、天使様はプレイヤーと直接会話できない（**`talk` 不可**）
- プレイヤーは一人で過ごすか、屋根裏の祈りに **そっと寄り添う** かを選ぶ
- 夕方の行動後 → **夜** へ進み、強制ナイトイベント（該当日）→ 毎晩ケアへ

#### 進行ルール（自由行動フェーズ）

1. 各 **朝 / 昼 / 夕方** に **行動を 1 つ** 選ぶ（または **様子を見る** → 戻る。時間は進まない）
2. 行動結果テキスト表示 →「続ける」
3. 時間進行: **朝 → 昼 → 夕方 → 夜**
4. **夜** に入ると、該当日の **強制ナイトイベント**（あれば）→ **毎晩ケア / マッサージパート** → 就寝 → **翌朝**（7 日目夜ケア後は終了）
5. **7 日目夜**（ケア含む）後は **モック終了**（`END_DAY = 7`）

**夜スロット:** 行動ボタンは出さない。昼または夕方の最後の行動 →「続ける」で夜へ移行する。

#### フェーズ（`gameState.phase`）

| 値         | 意味          |
| --------- | ----------- |
| `opening` | 1 日目固定シーン連鎖 |
| `free`    | 2 日目以降の自由行動 |
| `ending`  | モック終了画面     |

### コマンド（現行モック実装・旧版）

自由行動フェーズ（2 日目朝〜）でのみ表示。

#### 朝・昼の行動（5 ボタン）

| ID         | ラベル     | 役割                  |
| ---------- | ------- | ------------------- |
| `chores`   | 家事をする   | 教会の暮らし、役に立つ実感       |
| `pray`     | 祈る      | 天使様の日課への寄り添い        |
| `rest`     | 休む      | 何もしない時間、静けさ         |
| `talk`     | 話しかける   | 距離が縮む会話、callback の種 |
| `observe`  | 様子を見る   | 行動消費なし・時間進行なし。天使様の生活シチュを短文表示（§様子を見る） |

#### 夕方の行動（4 ボタン）

`talk` は **表示しない**（天使様が祈りの務め中のため）。

| ID        | ラベル   | 役割（夕方の意味） |
| --------- | ----- | ---------- |
| `chores`  | 家事をする | **晩御飯作り**（台所で一人で支度） |
| `pray`    | 祈る    | **屋根裏部屋**で、天使様の祈りの務めにそっと寄り添う |
| `rest`    | 休む    | **夜ケア前の余白回復**（個室で静かに休む） |
| `observe` | 様子を見る | 天使様が **屋根裏部屋にいる** ことを確認する短文 |

#### 様子を見る（`observe`）

| 項目 | 内容 |
| --- | --- |
| 消費 | **行動消費なし**（`actionCounts` 不変） |
| 時間 | **timeSlot 進行なし**（「戻る」で選択画面へ） |
| stats | **変動なし** |
| 本文 | `scenario-data.js` の `observeTexts`（キー: `d{N}_{timeSlot}`。夕方は `eveningDefault` / `d7_evening`） |
| フォールバック | `observeTexts.default` プールからランダム |

#### コマンド設計方針

- **増やしすぎない**。新コマンド追加は CURRENT_SPEC 更新＋理由必須
- 1 時間帯 **1 行動** のみ（`observe` は例外。行動後は自動で時間進行）
- 該当イベントがなければ **fallback** テキストで埋める
- オープニング中は **行動欄を出さない**（選択肢で進める）
- **本文は `scenario-data.js` に置く**。`app.js` に夕方・様子を見るの地の文を直書きしない

### ヒロイン状態

#### 表示用フィールド

| フィールド             | 用途                                         |
| ----------------- | ------------------------------------------ |
| `angelExpression` | 立ち絵プレースホルダー用（例: `tired`, `gentle`, `warm`） |
| `angelStatus`     | 短い状況説明（1 文程度）                              |
| `relationStage`   | 数値。会話・イベントで増減                              |

#### 距離感ラベル（`relationStage` → 表示）

| 値   | ラベル        |
| --- | ---------- |
| 0–1 | まだ遠い       |
| 2–4 | 少しずつ近づいている |
| 5–7 | 穏やかな信頼     |
| 8+  | 静かな安心      |

#### 状態更新の原則

- イベント・シーン定義に `angelExpression` / `angelStatus` / `relationChange` を書く
- **数値の意味を文章で補完** する（プレイヤーにはラベルも見せる）
- 表情 ID は将来 Live2D / 差分画像にそのまま流用できるよう **英小文字スネーク** で統一

### 会話と反応

#### 4 層構造（+ 毎晩ケア）

| 層        | データ                 | 用途                                  |
| -------- | ------------------- | ----------------------------------- |
| 固定オープニング | `openingScenes` + `sceneDefinitions` | 1 日目の物語。`choices[].next` で連鎖。初回ケア含む |
| 時間帯導入   | `slotIntroTexts`    | 自由行動 select 冒頭（朝昼夕） |
| 自由行動     | `freeActionEvents` + `freeActionTexts` | day × timeSlot × action の短い反応。夕方は `eveningDefault` フォールバック |
| 様子を見る   | `observeTexts`      | 行動消費なしの生活シチュ表示 |
| 強制ナイト    | `forcedNightEvents` + `nightEvents` | 特定日の夜（2・3・7 日目） |
| 毎晩ケア     | `nightCareEvents`（`night-care-data.js`） | 2 日目以降、毎夜必ず |

#### 時間帯導入文

- `slotIntroTexts` … 自由行動の **select** step 冒頭（例: `day2-morning`）
- **夕方:** `evening-default`（2〜6 日目共通）/ `day7-evening`（7 日目専用）。day 別キーが無い夕方は `evening-default` にフォールバック（`app.js` の解決ロジック）
- 新しい日を足すときは **導入文＋各 action の朝昼テキスト** をセットで追加。夕方は共通 `eveningDefault` で足りる日が多い

#### 夕方の自由行動（テキスト・データ）

夕方の本文は **`scenario-data.js`** に置く。`app.js` は解決・stats・ボタン出し分けのみ。

| データ | キー例 | 備考 |
| --- | --- | --- |
| `slotIntroTexts` | `evening-default`, `day7-evening` | 夕方 select 冒頭 |
| `freeActionTexts.{action}` | `eveningDefault`, `d7_evening` | chores / pray / rest 各ブロック内。`talk` は夕方なし |
| `observeTexts` | `eveningDefault`, `d7_evening` | 屋根裏部屋の気配を確認する短文 |

**2〜6 日目の夕方:** 各 action は `eveningDefault` にフォールバック。
**7 日目の夕方:** `day7-evening` / `d7_evening` で専用文（懺悔室イベント前の静かな気配）。

**stats（夕方・`app.js` 側・旧版数値。実装最新値は下記「angelFatigue の加算設計」DECISION_LOG 参照）:**

| 行動 | 効果の目安 |
| --- | --- |
| `chores`（晩御飯） | `caretakerAptitude` +1, `mentalMargin` -1（通常家事より控えめ。天使様不在のため `angelFatigue` は変化しない） |
| `pray`（寄り添い） | `prayerTuning` +3（低疲労時 +1 追加）, `mentalMargin` -1 |
| `rest` | 通常 `rest` と同じ（`mentalMargin` +2） |
| `observe` | 変化なし |

#### 会話の書き方

- **生活感・癒し・反応** を優先。長い説明より「一場面の温度」
- **短い callback**（前日の選択・会話を覚えている）を 1〜2 箇所に散らす
- 分岐は **フラグ 1〜2 個** で足りるように設計
- 主人公の内心は断定しすぎない。プレイヤーが距離を取れる余地を残す

#### 現 v0.2 の callback 例（実装済み）

| トリガー                | 内容                        |
| ------------------- | ------------------------- |
| `pain_tired`        | 3 日目夜「昨夜、少しだけ眠れていないと…」    |
| `hide_pain`         | 3 日目夜「大丈夫だと仰っていましたけれど…」   |
| `talked_with_angel` | 3 日目朝 talk「前に、そう仰っていましたね」 |

### 毎晩のケア / マッサージパート（現行モック実装・旧版）

本作では、**2 日目以降**の夜、自由行動フェーズにおいて **就寝前のケア / マッサージパート** を毎晩挟む。

これは **成人向け要素ではない**（現行モックの実装時点の方針）。全年齢の **ケア・疲労回復・ASMR 的な親密さ** として扱う。

> ※ Notion v0.4（本体）では販売方針が R-18版のみに転換済み。この節は現行 HTML モック（全年齢前提で書かれた実装）の記述をそのまま保全したもの。

#### 発生タイミング

- **2 日目以降**、夜の処理が終わった **後に必ず 1 回** 発生する
- 1 日目はオープニング内の `night_care` で **初回体験のみ**（自由行動フェーズ外）

```
昼または夕方の自由行動（最後の行動 → 続ける）
  ↓
時間帯が night へ
  ↓
強制ナイトイベント（該当日のみ。例: 2 日目疲れの問い / 3 日目 callback / 7 日目懺悔室）
  ↓
夜のケア / マッサージパート
  ↓
就寝
  ↓
翌朝へ（7 日目はモック終了）
```

**補足:** 夜に行動ボタンは出さない。上記は **夕方（または昼）の最後の行動のあと、夜に入ってからケアまで** の流れを指す。

#### 役割

| 目的     | 内容                                              |
| ------ | ----------------------------------------------- |
| 疲れの可視化 | 天使様が **夕方〜夜の祈りの仕事** で疲れていることを示す                 |
| 主人公の支え | 主人公が天使様を支える **就寝前の日課** にする                      |
| 同棲の親密さ | 同室生活の **静かな距離感** を出す                            |
| 反応の変化  | **日数** や `relationStage` によって、天使様の反応が少し変わる余地を残す |
| 将来拡張   | **親密イベント枠** に差し替え・追加できる構造にする                    |

1 日目オープニングの `night_care` は、このパートの **原型**。2 日目以降は本ルーティンとして定着する。

#### 描写方針（現行モック・旧版）

- **穏やか**、**静か**、**生活感** 重視
- **疲労回復** と **信頼関係の変化** を主役にする
- 説明しすぎない
- **露骨な描写にしない**
- **成人向け本文にしない**
- 触れ方・声・間など、ASMR 的な近さは **全年齢の範囲** で匂わせる
- **部位選択** や **ミニゲーム化** は **今はしない**

> ※ 本文の露骨さ・成人向けの扱いは Notion v0.4（R-18版のみ）で方針転換済み。夜ケアの成人向け本文はアーキテクチャ上 `text_adult` に差し込む（§全年齢 / R-18 差し替えアーキテクチャ）。この節は現行モック実装時点の描写方針として保全する。

#### 反応の変化（14 日版の目安・旧版）

`day` / `relationStage` / `nightCareCount` 等に応じて、段階的に変える。

| 段階  | 目安                        | 反応の例                        |
| --- | ------------------------- | --------------------------- |
| 初期  | 2〜3 日目、`relationStage` 低め | 少し遠慮する。「そんな……」              |
| 中盤  | 4〜8 日目                    | 自然に受け入れる。目を細める              |
| 後半  | 9 日目〜                     | 自分から少し頼む。「今日も、お願いできますか」     |
| 終盤  | 14 日目付近                   | この日課が **続いていく** 実感。言葉少なめの安心 |

3 日モックでは **日付別の固定 2 本**（2 日目・3 日目）で足りる。
**現行 7 日モック** では `night-care-data.js` に **1〜7 日目 + fallback** を用意。

#### 初期実装方針（v0.3）

- **毎晩 1〜2 文の短い固定イベント** でよい
- コマンド欄は **増やさない**（ケアは夜の自動進行。「就寝する」等で次へ）
- `forcedNightEvents` の **後**、翌朝へ進む **前** に挿入
- `nightCareEvents` 配列に day 別テキストを足すデータ駆動（**編集は `night-care-data.js`**）
- 初回モック: **2 日目・3 日目** 用に各 1 本 + fallback → **現行: 7 日目まで + fallback**

#### 就寝時回復

夜ケア後に **「就寝する」** を選ぶと、主人公の **心身の余白**（`mentalMargin`）が回復する。

##### 現在仕様（現行モック・旧版数値）

| 項目 | 内容 |
| --- | --- |
| 回復量 | 就寝時に `mentalMargin` **+2**（2026-07-14 の 2倍スケール化前の旧値。最新値は DECISION_LOG「angelFatigue の加算設計を実装」の仮置き数値表を参照） |
| 上限 | 既存の stats 上限に従う（`mentalMarginMax` を超えない） |
| 生活サイクル | 夜ケアで主人公が少し疲れ、就寝で回復する流れを作る |
| 通常画面 | 数値は表示しない。状態文と **今回の変化** コメントで伝える |
| デバッグ | 数値・変化ログで確認できる |

夜ケア本体の stats 効果（信頼・天使様の疲労・主人公の余白消費など）は、就寝回復とは **別タイミング** で適用される。

##### 将来拡張（未実装・旧版）

将来的には、夜ケアの **成果** によって以下を変動させる。

- 天使様の疲労回復量
- 主人公の余白減少量
- 天使様が主人公の疲れに気づくイベント
- ケアの質に応じたコメント差分

ただし **現時点では夜ケア成果判定は未実装**。
現在は固定処理として、夜ケア後の就寝で `mentalMargin` が回復する。

> ※ Notion v0.4「夜ケアパート仕様」では、この「将来拡張」がミニゲーム（持ちこたえる／ポジション4つ／おあずけ択）として仕様確定済み。ただし本体（HTMLモック）には**未統合**（別リポジトリでプロトタイプ開発中）。

#### データ構造（現行モック実装）

##### gameState

| フィールド              | 型        | 用途                              |
| ------------------ | -------- | ------------------------------- |
| `nightCareCount`   | number   | ケアを行った回数（2 日目以降の累計）。表示のたびに加算    |
| `careStyle`        | string   | 将来: ケアの傾向 ID（現状 `'default'` で可） |
| `seenNightCareIds` | string[] | 表示済みケアイベント id                   |

##### memoryFlags（夜ケア関連）

| フラグ                          | 初期値   | 用途                                  |
| ---------------------------- | ----- | ----------------------------------- |
| `first_night_care_done`      | false | 初回ケア完了（1 日目 `night_care` または初の毎晩ケア） |
| `night_care_routine_started` | false | 2 日目以降、毎晩ケアが日課として始まった               |
| `angel_relaxed_by_care`      | false | ケアで一度以上、明確に弛んだ反応があった                |
| `angel_asked_for_care`       | false | 天使様が自分からケアを求めた（後半用）                 |
| `confession_day_shared`      | false | 7 日目懺悔室で「少しだけ話す」を選んだ |
| `confession_day_uncertain`   | false | 7 日目懺悔室で「うまく言葉にならない」を選んだ |
| `confession_day_silent`      | false | 7 日目懺悔室で「何も言わずに座っている」を選んだ |
| `confession_day_avoided`     | false | 7 日目懺悔室で「今日はやめておく」を選んだ |

##### nightCareEvents（配列・`night-care-data.js`）

最小フィールド例:

```
id, day, priority, condition,
text, angelExpression, angelStatus, location,
setFlags, relationChange
```

- id と `seenNightCareIds` で管理し、将来 **親密イベント枠・差分イベント** に拡張可能にする
- セーブ対象: 上記 gameState フィールド + 新 memoryFlags を含める
- 1 日目 `night_care` 完了時に `first_night_care_done: true` を立て、2 日目以降と整合させる

#### 今はやらないこと（ケア関連・旧版）

| 項目           | 理由                     |
| ------------ | ---------------------- |
| 部位選択         | UI・分岐が複雑化              |
| 複雑なマッサージシステム | ミニゲーム・手順選択は MVP 後      |
| 成人向け差し替え     | 別レイヤーで将来検討             |
| 14 日分の詳細差分   | 7 日モック安定後に横展開          |
| 音声・画像の追加     | Live2D / ボイス / SE は後回し |
| ケア用コマンド追加    | 夜は自動進行のまま              |

> ※ 「複雑なマッサージシステム」「成人向け差し替え」は Notion v0.4「夜ケアパート仕様」で仕様確定済み（ただし本体HTMLモックには未統合）。この表は現行モック実装時点のスコープ外リストとして保全する。

### イベントの扱い（現行モック実装）

#### memoryFlags（初期値・全量）

```javascript
{
  angel_revealed: false,      // 天使開示済み
  went_to_attic: false,       // 屋根裏へ向かった（1日目・callback未接続）
  stayed_in_room: false,      // 部屋に留まった（1日目）
  respected_warning: false,   // 警告を守った（1日目）
  prioritized_help: false,    // 助けを優先（1日目）
  pain_tired: false,          // 2日目夜「少しだけ」
  hide_pain: false,           // 2日目夜「大丈夫です」→ 3日目夜 callback 分岐用
  helped_church: false,       // 家事系行動
  prayed_with_angel: false,   // 祈り系行動
  rested_in_room: false,      // 休息系行動
  talked_with_angel: false,   // 会話系行動
  remembered_by_angel: false, // 3日目夜 callback 済み（イベント側 setFlags）
  // --- 毎晩ケア ---
  first_night_care_done: false,
  night_care_routine_started: false,
  angel_relaxed_by_care: false,
  angel_asked_for_care: false,
}
```

#### gameState 追加フィールド（毎晩ケア）

```javascript
{
  nightCareCount: 0,
  careStyle: 'default',
  seenNightCareIds: [],
}
```

※ 実装に合わせた現状値。追加・改名時は **この一覧と app.js を同時更新**。

#### イベント選出（自由行動）

1. `action` が一致
2. `day` / `timeSlot` が一致（省略時はワイルドカード）
3. `condition(state)` が真
4. `priority` **降順** で 1 件採用
5. 該当なし → fallback（`priority: 1`）

#### seenEventIds

- オープニング: シーン `id`
- 自由行動: `free_{day}_{timeSlot}_{action}` 形式
- 強制夜: `forced_night_{day}`
- 毎晩ケア: `nightCareEvents[].id`（例: `night_care_d2`）

**未視聴優先の厳密ロジックは v0.2 では未採用**。日付・優先度・条件で足りる前提。

#### relationChange

- 多くのイベントで `+1`。大きく振らない
- オープニングの重要シーン（開示・食事・世話）も **+1 程度**

#### セーブ対象

- `gameState` 全体（`localStorage` キー: `angelChurchMockV0_1_save`）

### 日数構成の検討メモ（旧版・v0.3 時点。日数構成は Notion v0.4 で 28日本編に確定済み）

> **正式決定ではない（旧版当時の記述）。** 現在は Notion v0.4「日数構成（v0.4 確定：28日本編）」が正本。以下は現行 7 日モックが実装された時点の検討メモとして保全する。

当初は **14 日間**の短期任務として設計していた。癒しや生活への馴染みのため、最終版では **28 日構成にする可能性が高い**（検討中の有力方針）。

**暫定案（旧版）**

| 日 | 位置づけ |
| --- | --- |
| **14 日目** | 本来の **帰任予定日**。**選択フェーズではない** |
| **28 日目** | **本当の選択の日**（残留・帰任・関係の決断。旧案では 14 日目に置こうとしていた） |

**28 日 ≠ 毎日固有イベント**

- 28 日分すべてに大型イベントを置かない
- **重要日** と **通常日** を分ける
- 通常日は短い差分・状態文・覚えている一言で足す

**1 日のリズム（`CONCEPT.md` §4）**

- **朝・昼:** プレイヤーの傾向を蓄積（自由行動・会話選択）
- **夕方:** 天使様の祈りの務め。プレイヤーは一人で過ごすか、寄り添うか（§夕方の自由行動）
- **夜:** 強制イベント（該当日）・毎晩ケア。天使様に覚えてもらう時間を重視

**解禁・変化（将来仕様・未実装。旧版）**

- 日数だけでなく **パラメータ閾値**・**行動回数**（`actionCounts`）・**`memoryFlags`**・**選択比重** で会話・イベントを解禁
- 「話しかける」で選んだ内容を記憶し、後日に返す
- 選択比重で天使様の反応・返答を変える

**現時点の方針（旧版・現行モックの実装方針）**

- **7 日モックを維持・拡張**する（8 日目以降は未実装）
- 28 日化の正式判断は、**7 日モックの全体レビュー後**に行う（→ 判断済み。Notion v0.4 で28日本編に確定）
- 生活の蓄積感は **`memoryFlags` と短い差分テキスト** で表現する
- 「その後」は必ず描くが、**初回完成を優先**する

### エンディング候補（旧版）

#### 現モック

- **単一:** **7 日目夜** 毎晩ケア後 → 「モック終了」画面（本編 END ではない）

#### 28 日目で検討する方向性（旧版・v0.3 時点。当時は未実装・案のみ）

旧案では **14 日目エンディング** として扱っていた残留・帰任の分岐を、**28 日目の本当の選択** に置く想定。

| 方向           | 概要                       | 備考              |
| ------------ | ------------------------ | --------------- |
| 静かな定住        | 短期任務が終わっても、自ら残る選択        | テーマ「ここにいていい」に直結 |
| 使命として別れる     | 任務後、次の任地へ。ただし bond は残る | 切ないが余韻型         |
| 世話役として正式受け入れ | 教会側・天使様双方の合意             | 同棲継続のハッピー       |

- **複数 END の本格分岐は MVP 後**
- まず **1 本の「静かな安心」ルート** を通し切ることを優先
- **14 日目** は帰任予定の **物語上の節目** であり、ここでは選択させない（検討中。→ Notion v0.4 で「14日目＝偽りの結末」として確定）

### パラメータ設計（旧版・数値は最新でない）

> 数値は 2026-07-14 の2倍スケール化以前の旧値。最新の増減設計・数値は `docs/DECISION_LOG.md`「angelFatigue の加算設計を実装」を参照。

本作では、**28 日目の本当の選択**（当時：検討中）に向けた蓄積として、以下のパラメータを持つ。
**旧案（保留）:** 当初は **14 日目** に天使様から残留を提案する条件として設計していた。

**将来（旧版時点で未実装）:** パラメータ **閾値**・行動回数・`memoryFlags`・選択比重と組み合わせ、会話解禁・反応変化を行う（`CONCEPT.md` §5）。

#### クリア条件パラメータ（3 つ）

- 信頼
- 祈りの調律
- 生活支え度

#### 補助パラメータ

- 心身の余白
- 天使様の疲労

ゲームオーバーは設けない。
3 つのクリア条件を満たさなかった場合でも、優しい帰任エンドに進む（28 日目選択時の分岐想定。旧版時点で未実装）。

#### 信頼

天使様との距離を表す。
主に「話しかける」「夜ケア」「寄り添う選択」で上がる。
**28 日目**（当時：検討中）の「そばにいたい」系の選択に効く想定。**旧案:** 14 日目 END 条件。

#### 祈りの調律

天使様の務めにどれだけ寄り添えたかを表す。
主に「祈る」で上がる。
天使様の疲労が低いほど上がりやすい。
**28 日目**（当時：検討中）の「祈りを支えたい」系の選択に効く想定。**旧案:** 14 日目 END 条件。

#### 生活支え度

辺境教会の暮らしをどれだけ整えられたかを表す。
主に「家事をする」で上がる。
天使様の疲労を下げる効果も持つ。
**28 日目**（当時：検討中）の「この教会の暮らしを守りたい」系の選択に効く想定。**旧案:** 14 日目 END 条件。

#### 心身の余白

主人公の心身の余裕を表す。
主に「休む」で回復する。
夜ケア後の就寝でも回復する（就寝時回復 参照）。
家事・祈り・夜ケアなどで少し消費される。

- 初期値（旧版・2倍化前）: `mentalMargin` 5 / `mentalMarginMax` 10
- `mentalMargin` の上限は `mentalMarginMax` に従う（将来の成長用。現モックでは上限上昇処理なし）

心身の余白は、行動不能を生むためのスタミナではない。
余白が高いと家事・祈り・夜ケアの描写が落ち着いたものになり、効果が少し上がる。
余白が低い場合は、天使様が主人公の疲れに気づくイベントの種になる（痛みと癒しの記憶システム方針 参照）。

#### 天使様の疲労

天使様の疲れを表す。
夕方の務めで上がる。
家事・祈り・夜ケアなどで下がる。

天使様の疲労が低いと、祈りの調律が上がりやすくなる。
疲労が高い場合でもゲームオーバーにはならず、夜の反応や会話差分に反映する。

> ※ 2026-07-14 に「夕方の務めで上がる」加算処理が実装された。詳細・確定数値は `docs/DECISION_LOG.md`「angelFatigue の加算設計を実装」参照。

### 痛みと癒しの記憶システム方針（旧版メモ。6分類自体は Notion v0.4 で維持確定）

**現時点ではコード未実装。** 将来「痛みと癒し」をゲームシステムへ落とし込むための仕様メモ。
会話設計の詳細ルールは `AI_GUIDELINES.md` の「痛みと癒しの会話設計ルール」を参照。

#### 基本方針

このゲームでは、主人公の痛みを **直接選ばせたり**、**診断名のように表示したりしない**。

痛みは、日常会話、行動選択、夜イベント、休息、家事、祈り、夜ケアの中で **少しずつにじませる**。

目的は、痛みを 14 日間で解決することではなく、天使様との生活を通じて、**痛みの扱われ方が少し変わる** こと。

#### painSeeds

将来的に、主人公の痛みの傾向を内部的に薄く記録するため、`painSeeds` を導入する可能性がある。

| 候補 | 意味 |
| --- | --- |
| `uselessness` | 役に立てなかった痛み |
| `wantedness` | 必要とされたい痛み |
| `restlessness` | 休めない痛み |
| `belonging` | 居場所がない痛み |
| `selfBlame` | 自分を責める痛み |
| `grief` | 喪失や別れの痛み |
| `guardedness` | 対人警戒・距離を詰める怖さ |

**仕様方針**

- `painSeeds` は **内部値** として扱う
- 通常画面には表示しない
- デバッグ表示では確認できてもよい
- 初期段階では `painType` を確定させず、**傾向として蓄積** する
- 3 日目時点で明確に「あなたはこの痛みです」と **確定しない**
- 14 日版の中盤以降、必要なら `dominantPainType` を **仮判定** する
- `dominantPainType` も診断ではなく、**会話差分用の内部傾向** として扱う

> ※ Notion SESSION_STATE では「3日確定は7日モックの都合を引きずっている可能性があり、28日本編化した今は再検討中」（★大きめの仕様変更になる見込み）としている。最新の検討状況は `docs/SESSION_STATE.md` を参照。

#### memoryFlags 優先

`painSeeds` よりも、**具体的な選択や発言を覚える `memoryFlags`** を重視する。

例:

- `mentioned_tiredness`
- `said_cannot_rest`
- `hinted_transfer_or_exile`
- `admitted_loneliness`
- `mentioned_loss`
- `deflected_with_joke`
- `accepted_tea`
- `accepted_silence`
- `asked_to_be_left_alone`
- `thanked_angel`
- `night_care_opened_up`
- `night_care_stayed_silent`

後日回収では、抽象的な `painType` よりも、**具体的な `memoryFlags`** を使う。

例:

- 「昨日、少し眠れていないと仰っていましたね」
- 「昨日、何も話さずに座っておられましたね」
- 「あの椅子を、今日も選ばれたのですね」

#### 後日回収

後日回収は、天使様が「診断した」感ではなく、**「覚えていてくれた」感** を出すために使う。

**方針**

- 解決策を出さない
- すぐ結論を言わない
- 昨日の行動や言葉を **短く** 覚えている
- 事実として静かに返す
- 関係値を上げるためのご褒美ではなく、**安心感の演出** として扱う

#### painType の扱い

`painType` は便利だが、実装は **慎重に** 扱う。

**現時点の方針（旧版）**

- すぐには実装しない
- 3 日目で確定しない
- 使う場合も、診断ではなく **内部的な傾向** にする
- エンディングや夜ケア文の **一部差分** に使う程度に留める
- プレイヤーに「分類された」と感じさせない

#### 日数ごとの将来配置案（旧版・14日基準）

| 日数 | 方針 |
| --- | --- |
| **1〜3 日目** | 痛みを確定せず、生活会話と行動で `painSeeds` / `memoryFlags` を薄く拾う |
| **4〜7 日目** | 強い傾向があれば、自由行動や夜イベントに **軽い差分** を入れる |
| **8〜11 日目** | 夜ケアや会話で、**話す / 黙る / そばにいるだけ** の選択肢を出す |
| **12〜14 日目** | 問題解決ではなく、**少し息ができるようになった** ことを回収する |

#### 実装はまだしないもの（旧版）

- `painSeeds` の実装
- `painType` の確定処理
- `painProfiles` の大量実装
- `painNightCareTexts` の大量実装
- `painEndingHints` の大量実装
- 痛み別エンディング全文
- 診断画面
- 痛みタイプ選択 UI

### 呼び名入力

オープニング中、天使様が主人公に「あなたのことは、何とお呼びすればよいですか」と尋ねる。

プレイヤーは最大7文字で、天使様に呼んでほしい呼び名を入力する。

入力された呼び名は `playerCallName` として保存し、以後の会話文で使用できるようにする。

#### 仕様

- 入力は最大7文字
- 空欄の場合は「あなた」を使用する
- localStorage に保存する
- デバッグ表示で確認できる
- 本文中では `{playerCallName}` のようなプレースホルダーで差し込めるようにする

#### 特定ワード反応

特定の呼び名が入力された場合、天使様が専用の短い反応を返す。

例：

- 通常：
  - 「では、{playerCallName}さん、とお呼びしますね」
- 特定ワード：
  - 「……その呼び方で、本当によいのですか？」
  - 「少し、不思議な響きですね」
  - 「わかりました。では、そうお呼びします」

特定ワードと反応文は、後から編集しやすいデータ定義として管理する。

### 今はやらないこと（現行モック実装時点・旧版。最新版は Notion v0.4 本体「今はやらないこと」参照）

実装依頼時に **スコープ外** として明示する項目。

| カテゴリ   | 内容                                                               |
| ------ | ---------------------------------------------------------------- |
| 成人向け   | 露骨な描写、R18 シーン、年齢制限版分岐の **具体化・実装**                                |
| 表現素材   | Live2D、ボイス、BGM/SE 本番素材、背景・立ち絵の外注                                 |
| システム拡張 | 複数ヒロイン、ギャラリー、実績、装備、探索マップ                                         |
| 分岐     | 大規模フラグツリー、ルート分岐 3 本以上、ランダムイベント大量追加                               |
| ケア詳細   | 部位選択、複雑なマッサージシステム、成人向け差し替え、14日分詳細差分、ケア用コマンド                      |
| UI     | スマホ最適化、複数画面、設定画面の本格化                                             |
| インフラ   | サーバーセーブ、Steam 版、多言語                                              |
| 文書     | `DATA_MODEL.md` / `AI_GUIDELINES.md` / `NEXT_TASKS.md`（**別タスク**） |

> ※ 「成人向け」「ケア詳細の成人向け差し替え」は Notion v0.4（本体）で方針転換済み（R-18版のみを制作。ただし本文・CGの中身の具体化は引き続き作者専管でスコープ外）。この表は現行モック実装（v0.2/v0.3）が準拠していた旧スコープとして保全する。

### デバッグ機能

開発・検証用。本番プレイヤー向け機能ではない。

- デバッグ表示 ON/OFF 切替ボタンでパネル表示
- JSON 形式で `gameState` 主要フィールドを確認できる（`contentMode` 含む）
- 「モード: all_ages / r18」ボタンで全年齢/R-18 表示を実行時切替できる（検証専用。§全年齢 / R-18 差し替えアーキテクチャ）

#### デバッグ用OPスキップ

デバッグ表示 ON 時のみ、「**OPスキップ：2日目朝へ**」ボタンを表示する。

**目的**

- 1 日目オープニングを毎回通さず、2 日目朝から自由行動・パラメータ変化・夜イベント・夜ケアを確認するため

**仕様**

- デバッグ表示 ON 時のみ表示する
- デバッグ表示 OFF 時は表示しない
- 押すと 2 日目朝の自由行動へ進む
- 1 日目オープニング完了済み相当の状態にする
- `playerCallName` が未設定なら「あなた」を使用する
- `angel_revealed` / `first_night_care_done` など、2 日目朝に必要なフラグを設定する
- `stats` / `actionCounts` は初期値のままでよい
- 本番機能ではなく **開発補助機能** として扱う

### 既知の保留項目（現行モック実装時点・旧版）

| 項目                       | 状態         | メモ                                   |
| ------------------------ | ---------- | ------------------------------------ |
| **毎晩ケア文案の短文化**           | **v0.3方針** | 仕様上は 1〜2 文。14 日版で段階差分を増やす            |
| 1 日目夜の反応フラグ callback     | **未接続**    | `went_to_attic` 等は記録のみ。2 日目以降で拾うか要設計 |
| `hide_pain` フラグ          | **使用中**    | 2 日目夜「大丈夫です」→ 3 日目夜 callback         |
| 環境音 SE                   | **コメントのみ** | `slotIntroTexts` 等に「将来 SE」注記         |
| 14 日分のコンテンツ              | **未着手**    | 7 日モックのパターンを横展開                      |
| 本番 END                   | **未実装**    | 現状はモック終了画面のみ                         |
| 未視聴イベント優先ロジック            | **未採用**    | 必要なら spec 更新後に追加                     |
| 成人向けレイヤー                 | **未定（旧版）** | Notion v0.4 で R-18版のみに確定済み。本文差し込みは作者専管・未着手 |
| `pain_not_needed` 等の旧フラグ | **削除済み**   | v0.2 の `memoryFlags` に合わせる           |
| 成人向けレイヤーの**器**            | **実装済み**   | §全年齢 / R-18 差し替えアーキテクチャ 参照。本文差し込みは作者専管・未着手 |
| **痛み記憶システム**（`painSeeds` / `painType`） | **未実装・仕様メモのみ** | §痛みと癒しの記憶システム方針 参照。現行モックでは `painSeeds` / `painType` は未実装 |

### 全年齢 / R-18 差し替えアーキテクチャ（現行モック実装）

将来、DLsite向けR-18版と全年齢版を **同一データ構造から** 出力するための器。
モック段階ではスキーマと解決層のみ用意し、R-18本文・CGの中身は一切持たない
（本文差し込みは作者専管。データ上は `[TODO:作者差し込み]` プレースホルダのみ許可）。

#### 構成ファイル

| ファイル | 役割 |
| --- | --- |
| `content-config.js` | `window.CONTENT_CONFIG = { mode: 'all_ages' \| 'r18' }`。**モード切替はここ1箇所のみ** |
| `asset-resolver.js` | `window.AssetResolver`。モードに応じて normal / adult の参照を解決する層。DOM・gameState 非依存（Unity移植時に C# クラスへそのまま写す想定） |

読み込み順: `content-config.js` → `asset-resolver.js` → `scenario-data.js` → `night-care-data.js` → `app.js`

#### データスキーマ（同一IDによる差分管理）

- **テキスト**: エントリに `text_normal` / `text_adult` を持たせる
  - 従来の `text` キーは `text_normal` と同義（レガシー互換）。差分を持たないエントリは `text` のままでよい
  - 同一エントリに `text` と `text_normal` を混在させない（移行時は `text` → `text_normal` にリネーム）
  - `resultText` など別ベースキーも同じ規約（`resolveText(entry, 'resultText')`）
- **CG**: `{scene_id}_normal` / `{scene_id}_adult` の命名。adult 側の実在は `SCENARIO_DATA.cgRegistry`（定義済みCG IDの配列・任意）で判定する

#### フォールバック規則（差し込み前でも常に動く）

| モード | 解決順 |
| --- | --- |
| `all_ages` | `text_normal` → `text` |
| `r18` | `text_adult` → `text_normal` → `text` |

- `text_adult` が **未定義**、または **`[TODO:` で始まるプレースホルダ** の場合は normal 側へ自動フォールバックする
- CG も同様: `cgRegistry` に `{id}_adult` が無ければ `{id}_normal` を返す

#### app.js との接続

- 表示テキストの参照箇所は `resolveContentText(entry[, baseKey])`（AssetResolver の薄いラッパ）を通す
- `setFlags` / `relationChange` などの **処理・数値はモードで分岐しない**（差分は表示コンテンツのみ）
- モードはビルド設定であり **セーブデータに含めない**

> ⚠️ **状態棚卸しで判明した既知の乖離**：`content-config.js` の既定値は現状 `all_ages`。Notion v0.4「R-18版のみ」方針と逆になっている（DECISION_LOG「状態棚卸しの結果を受け、作業順序を『土台優先』に組み替える」参照）。`r18` 既定化は別タスクとして残っている（未着手）。

#### サンプル（動作確認用）

- `night_care_d3` … `text_adult` にダミー文（実内容ではないマーカー）。r18 切替で表示が変わることの確認用
- `night_care_d7` … `text_adult: '[TODO:作者差し込み]'`。プレースホルダが normal にフォールバックすることの確認用

#### デバッグ用モード切替

デバッグパネルに「モード: all_ages / r18」ボタンを表示し、実行時に切替・再描画できる。
検証専用であり、製品版のモードは `content-config.js`（Unityではビルド設定）で固定する。

#### 今やらないこと（旧版・アーキテクチャ関連）

- R-18本文・CGの具体化（作者専管）
- `textVariants`（memoryFlags 分岐）や選択肢ラベルの adult 差分（必要になった時点でスキーマ拡張）
- 全年齢/R-18でのイベント構成そのものの差し替え（差分は本文・CG参照のみ）

### scenario-data.js の役割

`scenario-data.js` は、制作者が後から編集しやすい **文章・表示データ** を管理する。`window.SCENARIO_DATA` として公開する。

**読み込み順（`index.html`）:**

```
scenario-data.js  →  night-care-data.js  →  app.js
```

`night-care-data.js` は既存の `window.SCENARIO_DATA` に `nightCareEvents` を **追加** する（夜ケア本文の編集はこちら）。

#### 現在管理しているもの（scenario-data.js）

| キー | 内容 |
| --- | --- |
| `callNameReactions` | 呼び名の特定ワード反応 |
| `statLabels` | stats 表示ラベル |
| `actionLabels` | 行動ボタン表示ラベル（`observe` 含む） |
| `timeSlotLabels` | 時間帯表示ラベル（`evening`: 夕方 含む） |
| `statusTexts` | stats 値に応じた状態文・雰囲気文 |
| `statusChangeTexts` | stats 増減時の雰囲気コメント（変化文） |
| `slotIntroTexts` | 時間帯導入文（`dayN-morning/noon`、`evening-default`、`day7-evening` 等） |
| `observeTexts` | 様子を見る短文（`default` プール、`dN_*`、`eveningDefault`、`d7_evening`） |
| `freeActionTexts` | 自由行動の結果テキスト（朝昼は `dN_*`、夕方は `eveningDefault` / `d7_evening`） |
| `openingScenes` | 1 日目オープニング本文 |
| `nightEvents` | 夜の強制イベント本文・選択肢・選択後テキスト（2・3・7 日目） |
| `endingTexts` | エンディング本文の置き場（分岐未実装） |

#### 現在管理しているもの（night-care-data.js）

| キー | 内容 |
| --- | --- |
| `nightCareEvents` | 毎晩ケア文（1 夜目オープニング・2〜7 日目・fallback） |

夜ケアを中心体験として作り込む拡張方針は `NEXT_TASKS.md` §夜ケア拡張案を参照（★DECISION_LOG「ドキュメント間の不一致」参照。`NEXT_TASKS.md` の記述と実装・Notion正本の間に矛盾があり、解消は別タスク）。

#### 今後追加予定（旧版）

- `openingTexts` の一部（オープニング外部化の続き）
- 8 日目以降の通常日・イベント
- 14 日版の夜イベント・自由行動テキスト
- 夜ケアミニゲーム用データ（`NEXT_TASKS.md` 参照）

#### app.js に残すもの

- `gameState`
- stats 変動
- `actionCounts`
- `memoryFlags`
- セーブ / ロード
- 画面描画
- 進行管理
- 条件判定
- callback

外部化は **1 回 1 機能** で行い、大規模リファクタは避ける。詳細は `PROJECT_BRIEF.md` §10・§12 も参照。

### 実装ファイル（参考）

| ファイル              | 役割                                      |
| ----------------- | --------------------------------------- |
| `index.html`      | 画面構造（タイトル / ゲーム / 終了）。スクリプト読み込み順を定義 |
| `style.css`       | 教会風 UI                                  |
| `content-config.js` | 全年齢/R-18 モード設定（`CONTENT_CONFIG.mode`）。最初に読み込む |
| `asset-resolver.js` | normal/adult 参照の解決層（`AssetResolver`。§全年齢 / R-18 差し替えアーキテクチャ） |
| `scenario-data.js` | セリフ・イベント本文・表示ラベル・状態文・夕方/様子を見るテキスト（§scenario-data.js の役割） |
| `night-care.csv`  | **夜ケア本文の編集元**（表計算で編集）。§夜ケアデータのCSV化 |
| `tools/build-nightcare.js` | `night-care.csv` → `night-care-data.js` 生成スクリプト（`node tools/build-nightcare.js`） |
| `night-care-data.js` | **夜ケア専用**データ（`nightCareEvents`）。**`night-care.csv` からの自動生成物**。手編集しない。scenario-data の後に読み込む |
| `app.js`          | 進行・状態管理・判定・保存・stats 処理。会話本文は直書きしない方針 |

#### app.js の編集ポイント

```
sceneDefinitions     … 1 日目固定シーン（会話追加・初回 night_care。本文の一部は scenario-data.js へ移行中）
slotIntroTexts       … 自由行動の時間帯導入
freeActionEvents     … 朝/昼/夕の行動反応（day, timeSlot, action, priority, condition）
forcedNightEvents    … 夜の強制イベント（day キー。本文は scenario-data.js の nightEvents）
nightCareEvents      … 毎晩ケア（本文は night-care-data.js。app.js は選出・進行）
actionDefinitions    … コマンドボタン定義（夕方は talk を除外して表示）
memoryFlagsDefault   … フラグ初期値（7 日目懺悔室: confession_day_* 含む）
```

#### scenario-data.js の編集ポイント

```
callNameReactions    … 呼び名の特定ワード反応
statLabels / actionLabels / timeSlotLabels … 表示ラベル
statusTexts          … stats 値に応じた状態文
statusChangeTexts    … stats 増減時の雰囲気コメント
slotIntroTexts       … 時間帯導入（dayN-morning/noon, evening-default, day7-evening）
observeTexts         … 様子を見る（default プール, dN_*, eveningDefault, d7_evening）
freeActionTexts      … 自由行動の結果テキスト（dN_* / eveningDefault / d7_evening）
openingScenes        … 1 日目オープニング本文
nightEvents          … 夜の強制イベント本文（2・3・7 日目）
endingTexts          … エンディング本文の置き場（分岐未実装）
```

#### 夜ケアデータのCSV化

夜ケア本文は **`night-care.csv`（表計算で編集）→ `tools/build-nightcare.js` で
`night-care-data.js` を生成** する。生成物は手編集しない。

- 列: `id, day, phase, countsAsRoutine, priority, location, angelExpression,
  angelStatus, relationChange, setFlags, text_normal, text_adult`
- 1 行 = 1 エントリ。text セルは空行（改行2つ）で段落分割される
- `setFlags` は JSON、数値列は空欄なら出力しない
- 再生成: `node tools/build-nightcare.js`
- 型が決まっている夜パートだけを対象にした運用。他パート（scenario-data.js）は
  当面 JS 直編集のまま
- **git安全**: `text_adult` 列には開発用の非センシティブなダミー/`[TODO:作者差し込み]`
  のみ置く。実 R-18 本文はこの追跡下CSVに書かない。実本文を入れる段階で
  adult 列を gitignore 対象の別CSV（`night-care.adult.csv`）へ分離する（§今後の課題 / NEXT_TASKS）
- 将来 stage/position 軸を足す場合も列追加で拡張できる（§全年齢 / R-18 差し替えアーキテクチャの差分軸）

#### 技術制約（現状維持）

- **外部ライブラリなし**
- `index.html` をブラウザで開くだけで動作
- データ駆動（配列・オブジェクトにイベントを足す形式）

### 変更履歴（現行モック実装・旧版）

| 日付         | 内容                                 |
| ---------- | ---------------------------------- |
| 2026-07-01 | 初版作成（モック v0.2 実装に基づく MVP 仕様固定）     |
| 2026-07-01 | 毎晩ケア / マッサージパート仕様追加                |
| 2026-07-01 | 毎晩ケア v0.3 仕様追記（役割・初期実装方針・今はやらないこと） |
| 2026-07-01 | パラメータ設計 v0.3 仕様追記                  |
| 2026-07-03 | scenario-data.js 外部化方針・役割を追記        |
| 2026-07-03 | statusChangeTexts・デバッグ用OPスキップ仕様を追記 |
| 2026-07-03 | 就寝時回復仕様を追記（mentalMargin +2・将来拡張方針） |
| 2026-07-03 | 痛みと癒しの記憶システム方針を追記（painSeeds / memoryFlags / painType） |
| 2026-07-03 | 日数構成の検討メモを整理（28日案は正式決定ではない旨を明記） |
| 2026-07-03 | CONCEPT.md 連携・28日/14日表現整理・将来解禁仕様を追記 |
| 2026-07-03 | 7日目モック・夕方スロット・様子を見る・night-care-data.js 分離・懺悔室イベントを反映 |
| 2026-07-12 | 全年齢/R-18 差し替えアーキテクチャを追加（content-config.js / asset-resolver.js / text_normal・text_adult スキーマ / フォールバック / 夜ケアサンプル差分） |
| 2026-07-12 | 夜ケアデータをCSV化（night-care.csv → tools/build-nightcare.js で night-care-data.js 生成）。実R-18本文のadult別CSV分離は今後の課題 |
