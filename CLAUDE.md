# CLAUDE.md — Claude Code 運用ガイド

辺境教会の天使様プロジェクト（`angel_church_mock_v0_1`）で Claude Code が守る手順。
**仕様の相談・決定はチャット側、実装は Claude Code。** Claude Code は勝手に仕様を決めない。

---

## このプロジェクトの分業

- **仕様の相談・決定**：チャット側（Claude Desktop / アプリ）
- **実装**：Claude Code（このリポジトリ）
- Claude Code は **1タスク1セッション**。タスクが完了したらセッションを終える（`/clear`）。次のタスクに進まない。
- 仕様の変更・追加が必要だと判断したら、**勝手に決めずに停止し、論点として提示する**。

### 状態管理ファイル（正＝Notion、ミラー＝ローカル）
| ファイル | Notion（正本） | 役割 |
| --- | --- | --- |
| `docs/SESSION_STATE.md` | [📍 SESSION_STATE](https://app.notion.com/p/39ca8a5dfd45811da793c644823551f7)（`39ca8a5d-fd45-811d-a793-c644823551f7`） | 現在の進捗・次にやること・未確定項目 |
| `docs/DECISION_LOG.md` | [⚖️ DECISION_LOG](https://app.notion.com/p/39ca8a5dfd4581eeb4a6f6ed085383bd)（`39ca8a5d-fd45-81ee-b4a6-f6ed085383bd`） | 設計判断の記録（却下案の蒸し返し防止） |

**Notion が正、ローカルの `docs/` 配下はミラー**（リポジトリに設計を残すための保険）。食い違ったら Notion を正とし、タスク完了時に Notion → ローカルの方向で両者を揃える。

---

## Notion 経由のタスク運用（実装依頼の流れ）

**今後、チャットと Claude Code のやり取りは Notion 経由で行う。**
実装タスクの指示は Notion のタスクページに置かれ、Claude Code はそれを名指しで読んで実装する。

### タスクの置き場所（天使教会プロジェクト）
| ページ | URL | 役割 |
| --- | --- | --- |
| 🗂️ _tasks（実装タスク） | [リンク](https://app.notion.com/p/39da8a5dfd4581af865be12e0f457c03)（`39da8a5d-fd45-81af-865b-e12e0f457c03`） | 実装タスク階層。設計ドキュメントとは分離 |
| 🟡 進行中 | [リンク](https://app.notion.com/p/39da8a5dfd458182832fc8571307aa5d)（`39da8a5d-fd45-8182-832f-c8571307aa5d`） | これからやるタスク。**ここの子ページを名指しで読む** |
| ✅ 完了済み | [リンク](https://app.notion.com/p/39da8a5dfd458174a19bdd354933f72e)（`39da8a5d-fd45-8174-a19b-dd354933f72e`） | 終わったタスクの置き場 |

### 流れ
1. **チャット側**がタスクページを作成（やること／やらないこと／制約／完了条件 等）。
2. **作者**が「Notion の〇〇タスクを読んで実装して」と Claude Code に指示。
3. **Claude Code** が該当タスクページを読む → 作業前に理解を1〜3行で確認 → 実装。
4. 完了したら **タスクページの「実装結果」セクションに追記**（変更ファイル／確認手順／決めたこと／詰まった点／完了条件の結果）し、`docs/*.md`（必要なら `CURRENT_SPEC`）を更新。
5. **チャットには短い完了報告のみ**返す（詳細は Notion が実体）。

> 原則：**チャットは「通知」、Notion が「実体」。** 同じ内容を二重に書かない。
> 1タスク＝1ページで物理的にスコープを区切る（最小差分・1回1機能を守るため）。
> 設計ページを丸ごと読ませない（却下案・将来構想まで拾ってスコープが広がるため）。

### タスクページに必ずあるセクション
やること（スコープ）／やらないこと（除外）／制約／完了条件（チェックリスト）／完了時にやること／背景（必要時のみ）／**実装結果**（Claude Code が記入）。

### 完了→「完了済み」へ移す前の確認（情報を失わないため）
1. 設計判断が `docs/DECISION_LOG.md` に転記されているか
2. 進捗・次の作業・未確定が `docs/SESSION_STATE.md` に反映されているか
3. 確定仕様が `CURRENT_SPEC.md` または設計ページに反映されているか
→ 3つが満たされていればタスクページは削除しても安全。

---

## セッション開始時の手順

1. **`CLAUDE.md`（本ファイル）を読む**
2. **`docs/DECISION_LOG.md` を読む** — ここで**却下済みの案は再提案しない**。設計思想を引き継ぐ。
3. **`docs/SESSION_STATE.md` を読む** — 現在地・次にやること・未確定項目を把握する。
4. **指定された Notion タスクページ（「進行中」配下）を読む** — 実装依頼はここにある（§Notion 経由のタスク運用）。
5. 参考（必要に応じて）：`CURRENT_SPEC.md`（実装仕様）、`CONCEPT.md`（体験の核）、`AI_GUIDELINES.md`（作業ルール）、`CHARACTER_DESIGN.md`（キャラ設計）、`NEXT_TASKS.md`。
6. **今回のタスクの理解を1〜3行で述べる**（受け止め違いをここで潰す）。
7. 実装は **1機能ずつ → 動作確認 → コミット**。大規模リファクタは禁止。
8. R-18 テキスト・CG の中身は書かない（`[TODO:作者差し込み]` プレースホルダのみ）。センシティブ素材は git にコミットしない（`.gitignore` 済）。

---

## セッション終了時（タスク完了時）の手順

タスクが完了したら、**ローカル md と Notion の両方を更新**してから終える。

1. **Notion タスクページの「実装結果」セクションに追記** — 変更ファイル／確認手順／決めたこと／
   詰まった点／完了条件の結果を書く（§Notion 経由のタスク運用）。
2. **チャット側が Notion の DECISION_LOG / SESSION_STATE（恒久ページ）に今回の判断・進捗を反映する**のを待つか、
   その場で反映を依頼する。
3. **`docs/DECISION_LOG.md` と `docs/SESSION_STATE.md` を Notion の内容に合わせて更新する**（Notion → ローカルの
   方向で同期。逐語で転記し、要約しない。ローカルにしかない情報は削除せずマージする）。
4. **`CURRENT_SPEC.md` など、必要な仕様ドキュメントも Notion の該当ページに合わせて更新する。**
5. 変更を **コミット**（必要ならブランチに push）。コミットメッセージは簡潔に、日本語で可。
6. **チャットには短い完了報告のみ返す。セッションを終える。次のタスクを始めない。**

> 更新の向き：**正本は Notion。** ローカルの `docs/*.md` はミラー（リポジトリに設計を残すための保険）。
> タスク完了時は **Notion → ローカル** の向きで揃える。

---

## モデル選択

- 日常＝**Sonnet 5**、難所＝**Opus 4.8**。
- **Fable 5 は原則使わない**（2026-07-13 よりサブスク定額枠から外れ完全従量課金のため）。

---

## やってはいけないこと（クイックリファレンス）

- 却下済みの案を再提案する（→ `DECISION_LOG.md` を必ず読む）
- 仕様を勝手に決める・増やす（→ 停止して論点提示）
- 1セッションで複数タスクを連続処理する（→ 1タスク1セッション）
- R-18 の本文・CG 内容を書く／センシティブ素材を git に載せる
- ローカル md を更新せずにセッションを終える
