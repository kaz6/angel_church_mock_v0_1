<!-- Notionミラー元: https://app.notion.com/p/39ca8a5dfd45811da793c644823551f7 (📍 SESSION_STATE（現在地）) -->

> **このページの役割**
> 「現在の進捗」「次にやること」「未確定項目」を1枚で把握する。
> **★ この Notion ページが正本。** ローカルの `docs/SESSION_STATE.md` は**ミラー**（リポジトリに設計を残すための保険）。
> Claude Code はタスク完了時、**Notion → ローカル**の方向で同期する。
> チャット側で新しい相談を始めるときは、まずこのページを読む。

**最終更新：2026-07-15（angelFatigue の加算処理を実装＝往復するようになった（土台①）。次は28日シミュレーションで数値決め）**

---

# 現在の進捗

## 確定済み（詳細は DECISION_LOG 参照）

### 販売・構成
- **販売方針**：R-18版のみを制作。DLsite先行。Steamは対象外（全年齢版は当面作らない。技術baseは維持）
- **日数構成：28日を本編として確定**（14日MVPは撤回）。14日目＝**偽りの結末**として本編内に組み込む（D-16）
- **CURRENT_SPEC v0.4 が仕様の正本**（販売方針・28日構成・夜ケア・キャラ記号・表情システム・「今はやらないこと」を反映済み）

### 夜ケア
- **ゲーム性を確定**：コアの動詞＝**「持ちこたえる」**（抗うではない）。耐えた量＝ angelFatigue の回復量。ミニゲーム1種のみ（主導権の反転なし）。ゲームオーバーなし・精度判定表示なし
- **ポジション4つ**：進行で置き換わるのではなく**選択肢として累積**し、以降いつでも切替可能。**性能差なし**
- **「今夜の求め」**：ポジションが2〜3に増えた段階から**時折**発生。開始前の立ち絵会話で**それとなく**ヒント。応えれば**僅かな**ボーナス。応えなくてもペナルティなし
- **全28夜がケア対象＋「おあずけ」択**：おあずけ＝ angelFatigue が回復しない（翌日の余白が減る）が、**天使様は咎めない**
- **オートは2種に分離**：テキスト送り＝通常採用。ミニゲームのオート＝採用するが、結果を**「平均的な回復」に固定**
- **絵は1枚絵ベース（U-07 クローズ）**：ベース4枚（ポジション別）× 差分5枚。差分は「表情バリエーション」ではなく**状態連動**として持つ
- **動機は前半後半で入れ替わる（D-20）**：天使様の疲労は **14日目前後で概ね取り除ける**。前半＝回復させるためにやる（結果は angelFatigue に乗る）、**後半＝回復の必要はないが、それでもやる＝行為自体が目的**（結果は**別の値**に乗る）。転換は台詞で明示する
- ⚠️ **厳守**：夜ケアで回復するのは **angelFatigue（天使様の疲労）**であって、**主人公の painType ではない**。この2つを混ぜない（アンチパターン「痛みの数値ゲージ化とゼロ到達＝クリア」に抵触させない）
- 詳細は「🌙 夜ケアパート仕様（v0.1）」

### キャラクター・ビジュアル
- **キャラクター記号**：シスター服ベース／**既存の宗教モチーフ（†✡等）は一切使わない**／糸目／目の隈／**背中の衣服に横一線の切れ目**（夕方の祈りで翼が出る）／翼＝白・先端が黒／髪は長め
- **時間帯**：夕方＝天使様のお仕事（祈り）の時間。**正体判明も夕方**
- **開眼の扱いを確定（D-14）**：立ち絵の目は**糸目/半目のみ**。開眼は表情システムに含めず、**夜パート／核心の1枚絵（イベントCG）側で表現**する。使用回数の制限は設けない
- **表情の段階数を確定（U-03 クローズ / D-18）**：立ち絵1ポーズ = **11レイヤー**
  - 目：3枚（糸目 / 半目 / **妖艶半目**）
  - 隈：2枚（薄 / 濃・**αクロスフェードで無段階に見せる**）
  - 感情セット（口・眉・頬・涙）：6枚（平常/笑/大笑/怒/悲/驚）
  - **妖艶半目は特別プリセット限定・終盤中心・全28日で3〜5回**。判断基準＝**「天使様が主人公を必要としていることが漏れる瞬間」**（時間帯ではなく意味で縛る）
- **隈と数値の結線を確定（U-02 クローズ / D-17）**：kumaLevel は状態変数にせず、**angelFatigue からの導出値**とする。長期変化は**疲労蓄積レートの低下**で表現。昼の立ち絵＝**朝の値でスナップショット固定**、夜のCG＝**リアルタイム値**。OPはシステム外（直接指定可）

### 数値・パラメータ
- **`angelFatigue` の加算設計を確定**：
  - 加算：**夕方の祈り（天使様の日課）で大きく** / **昼に主人公が「休む」と少し**（天使様が家事をするため）
  - 減算：**昼の「家事」** / **夕方の「祈る」**（調律が上がる＋疲労の上昇をほんの少し抑える） / **夜ケア**（本筋。ミニゲームの結果で回復量が変わる）
  - **`trust` が上がると、疲労の蓄積量が減っていく**（加算量全体に効く）。これが D-17 の「疲労蓄積レートの低下」の実体。**新変数ゼロ**
  - 現在の数値は14日前提のため、**一旦すべて２倍**（angelFatigue の範囲 0–10 → **0–20**）。最終値は28日シミュレーションで決める
  - **上限到達時は描写のみ。ペナルティは科さない**（隈が最大／台詞が短くなる／反応が薄くなる）。`trust` は下げない

### 体制・運用
- **開発ワークフロー**：仕様＝チャット、実装＝Claude Code、1タスク1セッション
- **Claude Code とのやり取りは Notion 経由**で行う（コピペ事故とチャット履歴消失の回避）
- **Notionページの運用ルール**：恒久ページ（SPEC / 夜ケア仕様 / DECISION_LOG / SESSION_STATE）は上書き更新して消さない。使い捨てページは `TASK：` `WIP：` プレフィックスで識別し、**結論が DECISION_LOG か CURRENT_SPEC に転記されたら削除してよい**。圧縮はしない（残すか消すかの二択）
- **イラスト外注方針**：発注先1人、探索軸は線と色、発注書テンプレ作成済み

## 実装状況
- HTMLモック本体：1〜7日目まで通しプレイ可能。セーブ/ロード、懺悔室、memoryFlags 実装済み
- 夜ケア単体プロトタイプ：別リポジトリで開発中（ミニゲームは本体未統合）
- 全年齢/R-18 差し替え基盤：実装済み（`content-config.js` / `asset-resolver.js`）。当面 r18 固定
- 夜ケアCSVパイプライン：実装済み
- gitガードレール：実装済み
- **angelFatigue の加算を実装（土台①・2026-07-15）**：夕方の祈り（日課・每日1回）＋昼restで加算し往復化（単調減少を解消）。trustで加算量が減衰。上限はclampのみ・ペナルティなし。増減は app.js の STAT_CONFIG/FATIGUE_CONFIG に集約。stats全体を2倍スケール化。→ ▶本体の単調減少問題は解消済み（上の⚠️メモは古い）。詳細は DECISION_LOG「2026-07-14｜angelFatigue の加算設計を実装」

> ✅ **状態棚卸し完了**（「📦 STATE_INVENTORY」参照）。以下が判明した：
> - **`painPoints` / `painType` / `painSeeds` はコードに0件**。`pain_tired` / `hide_pain` の boolean 2つのみ。**6分類は一度も実装されていない**（→ 捨てるものがない。実装前に直せる最良のタイミング）
> - ⚠️ **`angelFatigue` が単調減少。増加させる処理がコードに存在しない**。夜ケア設計の土台が欠けている
> - ⚠️ **ドキュメント間で不一致**：`NEXT_TASKS.md` が夜ケアの数値仕様（careStage 1〜5 等）を「確定済み」と記述。実装には無く、CURRENT_SPEC は「未実装」と書いている
> - `content-config.js` の既定が `all_ages`（R-18のみ方針と逆）
> - `careStyle` / `angel_asked_for_care` / `mentalMarginMax` は定義のみで未使用
> - `angelExpression` は自由記述文字列。新表情システムへの移行時に棚卸しとマッピングが必要

---

# 次にやること

## 優先度：高
1. **状態棚卸しの結果を確認する**（`docs/STATE_INVENTORY.md`）
   - 仕様書と実装の乖離を把握する。これが済むまで、痛みタイプ関連の再設計は判断しない
2. **痛みタイプ6分類の再検討**（★大きめの仕様変更になる見込み）
   - 論点：**「3日目に painType を1つに確定させる」のは尚早ではないか**。人の痛みは1つではない。3日縛りは7日モックの都合（END_DAY=7）を引きずっている可能性が高く、28日本編になった今、制約は消えている
   - 検討中の方向：painPoints（6軸ベクトル）を潰さず持ち続ける／`painType`（単数確定）を廃止し `primaryPain`（導出値・変わりうる）へ／`painSeeds` を Map にして**複数の痛みが別々の段階に同時にいられる**ようにする
   - コストの正体：**テキスト爆発**。書ける本数に上限を設ける必要がある（例：primary 1本はフルアーク、secondary 1〜2本は exposed 止まり、残りはテキストにしない）
   - **棚卸し結果を見てから判断する**
3. **後半用の値を定義する**（何を表す値か／絵にどう出るか／更新ルール）
   - 「置く」は確定済み（D-20）。**何を表す値にするかが未定**
   - **制約：絵に出る値でなければならない**。後半は angelFatigue が0で張り付くため隈が動かない。差分5枚をすべて fatigue に連動させると**後半の絵が固まる**
   - 駆動軸の分担案（発注枚数は増えない）：**隈 ← angelFatigue（前半）／表情・汗など残りの差分 ← 新しい値（後半）**

## 優先度：中
1. **髪色・差し色の決定** — 決まればキャラ設定シートが完成し、発注に進める
2. **発注枚数の見積もりを確定**（立ち絵 11レイヤー/ポーズ ＋ 夜CG ベース4枚×差分5）
3. **イラストレーター探索の開始**（探索軸：線の勢い＋限定パレット＋日常と崩しの両立）
4. **プロジェクトハブのトップ記述**は v0.4 に更新済み。ただし**ページタイトルが「辺境教会の天使様と、はじまりの生活」のまま**（CLAUDE.md は「天使様と辺境教会」）。仮題の統一は別途

## 保留
- 全年齢版の制作（必要になったら改めて判断）
- 他プロジェクトへの外注予算配分

---

# 未確定項目

| 項目 | 状況 | 備考 |
| --- | --- | --- |
| **`angelFatigue` の具体数値** | 実測で決める | 設計は確定済み。**数値は28日シミュレーションで決める**。最初に見るべきは、**夕方の祈りの加算量 と rest/chores/pray の増減の比率**。祈りが大きすぎるとプレイヤーの行動が無意味になり、小さすぎると天使様の孤独が伝わらない |
| **上限到達時の描写** | 未定 | 「様子が変わる」を具体化する（隈が最大／台詞が短くなる／反応が薄くなる）。**表情システムと台詞の両方に影響**する |
| **痛みタイプの構造（6分類・3日確定）** | 再検討（土台の後） | 棚卸しの結果、**コードに0件＝一度も実装されていない**。捨てるものがないため、落ち着いてやればよい |
| **後半用の値（関係の深まり）** | ★最優先 | 「置く」は確定（D-20）。**何を表す値にするかが未定**。制約：絵に出る値であること |
| **疲労蓄積レートの低下幅** | 実測で決める | 28日シミュレーションで当たりをつける。★**前提：`angelFatigue` の加算処理が先に必要** |
| **髪色** | 未定 | 差し色とセットで決める |
| **差し色** | 未定 | 限定パレット前提。琥珀＋藍 / セピア＋朱 などが候補 |
| **差分5枚の具体軸** | 未定 | 汁量・汗・目のハートマークが候補。状態連動にすることは確定。**駆動軸を前後半で分ける必要がある** |
| **夜CGの衣装状態** | 未定 | 選択肢にすると乗算で増える。**ポジションごとに決め打ち**が推奨。眼鏡差分はレイヤー1枚で作る方針 |
| **夜ケアの数値（ゲージ速度・判定幅）** | 未定 | プロトタイプで実測して決める |
| **ポジションの解禁条件** | 未定 | 日数 or 進行フラグ |
| **「今夜の求め」の頻度・伝達方法** | 未定 | 立ち絵会話で「それとなく」ヒントを与えることは確定。具体の台詞・発生率は未定 |
| **おあずけ時の回復量** | 未定 | 0 か微量か。咎めないがコストはある、というバランス |
| **28日構成の後半（15〜28日目）** | 骨組みのみ | 本実装はUnity側。**15日以降は恋愛シム側にも振る方針**（作者判断） |
| **キービジュアルの構図** | 未定 | 発注書テンプレの「5. 構図要件」が空欄 |
| **イラストレーター** | 未定 | 探索未着手 |
| **DLsite以外の販路** | 未定 | Fanza・BOOTH等。クレカリスク分散のため |

---

# リスク

| ID | 内容 | 緩和策 |
| --- | --- | --- |
| **R-01** | **スコープ倍増**（28日確定に伴う）。14日で一度着地させる安全弁を失った | 後半14日は重要日を絞り、通常日＋短文プールの比率を上げる。「何も起きない日」の許容という既存の設計原則をそのまま緩和策として使う。進捗が想定を下回った場合、後半の重要日をさらに削る余地がある |
| **R-02** | **後半14日の執筆負荷**。夜は「疲れていないのに求める天使様」を書き切る必要があり（求める理由を疲労に逃がせない）、難易度が上がる。加えて**昼も恋愛シム側に振るため分量が増える**。二重の負荷 | 増えるのは量ではなく難易度であることを自覚し、後半の重要日を絞る。惰性の反復になっていないか、都度確認する |
| **R-03** | **仕様書と実装の乖離**。仕様書だけが一人歩きしている箇所がある可能性 | 状態棚卸し（`docs/STATE_INVENTORY.md`）で実態を把握する。以後、仕様変更のたびに実装との突き合わせを行う |
| **R-04** | **ドキュメント間の矛盾**。`NEXT_TASKS.md` が夜ケアの数値仕様を「確定済み」と記述しているが、実装には無く、CURRENT_SPEC は「未実装」と書いている | **DECISION_LOG が正**であることを他ドキュメントにも徹底させる。土台作業の②で解消する |
| **R-05** | **正本とミラーの逆転**。Notion にはチャット側の判断が蓄積されているが、ローカル `docs/*.md` は旧版のままだった | **Notion を正本に反転**（2026-07-15）。Claude Code がタスク完了時に Notion → ローカルへ同期する。**次のタスクで docs 同期を実施** |

---

# Claude Code へのセッション開始手順
1. `CLAUDE.md` を読む
2. `docs/DECISION_LOG.md` を読む（**却下済みの案は再提案しない**）
3. `docs/SESSION_STATE.md` を読む
4. 今回のタスクの理解を1〜3行で述べる
5. 1機能ずつ実装 → 動作確認 → コミット
6. **タスクが完了したらセッションを終える。次のタスクに進まない**
7. 完了時、`DECISION_LOG.md` と `SESSION_STATE.md` を更新し、**Notionの同名ページにも反映する**

**仕様の変更・追加が必要だと判断した場合、勝手に決めずに停止し、論点として提示すること。仕様決定はチャット側で行う。**
