<!-- Notionミラー元: https://app.notion.com/p/39ca8a5dfd4581eeb4a6f6ed085383bd (⚖️ DECISION_LOG（設計判断の記録）) -->

> **このページの役割**
> 設計判断を記録し、**却下した案の蒸し返しを防ぐ**。AIアシスタントを乗り換えても設計思想ごと引き継げるようにする。
> **★ Notion ページが正本。** ローカルの `docs/DECISION_LOG.md` はミラー（リポジトリに設計を残すための保険）。Claude Code はタスク完了時、**Notion → ローカル**の方向で同期する。
> （注: Notion 側 DECISION_LOG ページの役割メモは旧方向「ローカルが正」のまま残っている。Notion 側の修正は別途。）

## 記録フォーマット
各エントリは以下を必ず含める。
- 何を決めたか
- なぜそう決めたか
- 検討したが却下した案と、その理由
- 参考にした先行事例や根拠

---

# 2026-07-13｜販売方針：R-18版のみを制作する

**決定**
R-18版のみを制作し、DLsite先行で出す。全年齢版は当面作らない。Steamは対象外。

**理由**
- 昼のチルさと夜のギャップが商品の本体であり、夜のパートを外すと作品が成立しない
- 2025年7月以降、Steamは決済代行業者・カード会社の基準を理由に成人向けゲームの削除を継続している。Valve自身が「決済手段を失うと他タイトルも購入できなくなるため」と説明
- 2026年4月には、成人向けゲームの売上送金が国内銀行に停止された事例も報じられている

**却下した案**
- **全年齢版を先に完成させ、後からR-18版を作る**：却下。作品の核が夜のギャップにあるため、全年齢版は「本体を欠いた版」になる。制作リソースも二重にかかる
- **Steamで成人向けとして配信**：却下。決済リスクが高すぎる

**未解決の依存**
- DLsiteも2024年からクレカ問題の影響下にある。販路を1本に依存せず、Fanza・BOOTH等も視野に入れる

---

# 2026-07-13｜全年齢/R-18 差し替えアーキテクチャの位置づけを変更

**決定**
差し替えアーキテクチャ（`text_normal` / `text_adult`、AssetResolver、モード切替）は**実装済みのまま維持するが、当面は r18 モード固定で運用する**。

**理由**
- 実装コストは既に支払い済み。削除する方が手間がかかる
- 将来的に全年齢版を出す判断をした場合、スキーマを後から足すのは全シーンデータの手直しになり高コスト

**却下した案**
- **差し替え機構を削除して単純化する**：却下。将来の可逆性を失う割に、得られる単純さが小さい

---

# 2026-07-13｜キャラクター記号の確定

**決定**
- シスター服ベース。**既存の宗教モチーフ（†・✡等）は一切使わない**
- **糸目**。昼は糸目、夜/核心の場面で目が開く
- **目の隈**。物語の進行とケアの結果で増減する
- 表情差分は **「感情セット（喜怒哀楽）＋ 目の開度（糸目/半目）＋ 隈の濃さ」** で設計する（2026-07-13 更新。詳細は後掲「表情設計を『感情セット＋目の開度＋隈』に再構成」参照）
- **背中の衣服に横一線の切れ目**。通常は意匠に見えるが、夕方の祈りの時にそこから翼が出る。冒頭のハプニングで判明する
- 髪は長め

**理由**
- 糸目と隈は一見両立しないが、**「昼＝糸目／夜＝開眼」の対比構造にすることで、作品の核である昼夜のギャップそのものを表情システムに載せられる**
- 糸目のまま隈が隠れ、開眼した瞬間に疲弊が露わになる、という演出が成立する
- 宗教モチーフを使わないことで、DLsiteの一覧面で「よくある堕天使モノ」に埋没するのを避ける
- 背中の切れ目は「普段は服の意匠に見え、後から意味が判明する」伏線として1枚絵に忍ばせられる
- 髪が長い理由：①梳くシチュエーションが作れる ②翼が出る背中を髪が隠す/かき上げる演出が生まれる

**却下した案**
- **輪（halo）・翼を聖性の記号として前面に出す**：却下。他の天使モノに埋没する。この作品の天使は「万能に救済する天使」ではなく「疲弊した天使」であり、そこが差別化点
- **喜怒哀楽を"大きな表情"（目を見開く等）で切る**：却下。この天使は本気で怒らず、糸目が基本。感情は糸目/半目を基盤に口・眉・頬で載せ、開眼は核心の特別カットに限定する（2026-07-13 更新。後掲項参照）

**未確定**
- 髪色
- 差し色

---

# 2026-07-13｜イラスト外注の方針

**決定**
- 発注先は**1人**（R-18まで一貫して依頼する）
- 探索軸は**線と色**：①線に勢いとかすれがあるか ②限定パレットを使えるか ③日常の温度と崩し/狂気の両方を描いた実績があるか
- 発注前に**天使様のビジュアル記号を作者側が言語化して渡す**

**理由**
- R-18を受ける絵師は全年齢のキービジュも描けるが、逆は成立しない。1人に統一しておく方が選択肢が残る
- 参照している作家性（線の勢い＋限定パレット）は、**記号を渡すと爆発するタイプ**であり、「かわいいシスターを描いて」と丸投げすると弱い成果物になる

**却下した案**
- **天使様用とSteam組（他3本）で絵師を分ける**：一旦却下。当初は「参照作家＝狂気/ダーク軸だからいやしけいには使えない」と判断したが、追加の参考画像で**同じ作家性が日常の温度も描けること**が判明したため、軸を1本に統合した

**契約上の必須項目**
- 著作権譲渡（27条・28条を含む）
- **著作者人格権不行使特約**（Steamカプセルやサンプル画像のトリミング・調整に必須。人格権は法律上譲渡できないため、特約がないと改変が差し止められうる）
- 二次利用範囲の明記（ストア・SNS・広告・動画サムネ・出展・グッズ・続編）
- AI学習用途への提供禁止

---

# 2026-07-13｜開発ワークフローの分離

**決定**
- **仕様の相談・決定はチャット側**（Claude Desktop/アプリ）で行う
- **実装はClaude Code**で行う
- Claude Codeは **1タスク1セッション**。完了したら `/clear` する
- Claude Codeが仕様変更の必要を感じたら、**勝手に決めずに停止して論点として提示する**
- ローカルの `docs/` 配下のmdが正、Notionはミラー

**理由**
- 長時間セッションの後半で、初期に決めた前提がコンテキストから押し出され、精度が静かに劣化する現象が実際に発生した
- サブエージェントは親の文脈を持たないため、タスク指示文が自己完結していないと機能しない
- 「1機能ずつ、動いたらコミット」という既存の開発規約が、そのままセッションの区切り単位として最適

**却下した案**
- **ChatGPT Work + n8n + Notion で要件定義と実装を分業する**：却下。①コスト削減の前提が成立しない（サブスク内で完結する作業を有料APIに逃がす形になる）②n8nの保守コストがソロ開発には重い ③別モデル系を挟むと文脈の損失がむしろ増える
- **Claude Code に Notion MCP で毎回仕様を読ませる**：却下寄り。トークン消費が大きい。リポジトリ内のmdを正とし、Notionは人間とチャット側が読むミラーとする

**モデル選択**
- 日常＝Sonnet 5、難所＝Opus 4.8。**Fable 5は原則使わない**（2026-07-13よりサブスク定額枠から外れ、完全従量課金に移行したため）

---

# 2026-07-13｜表情設計を「感情セット＋目の開度＋隈」に再構成

> 上記「キャラクター記号の確定」の表情軸を更新する項。旧方針「目の開度×隈のみ／喜怒哀楽は却下」を差し替える。

**決定**
- 表情は **感情セット（喜怒哀楽）を採用**する。ただし糸目・半目をベースに、**口・眉・頬**で感情を出す（大きく目を見開く方向には振らない）。
  - 笑顔（糸目/半目・口元笑顔）
  - 大笑顔（大きく口を開く・笑い泣きの涙マーク案あり）
  - 怒り（糸目・頬膨らませ ＝ 本気の怒りではない）
  - 悲しみ（糸目/半目〔本気の悲しみのときのみ半目〕・口元への字・眉にしわ）
  - 驚き（糸目/半目・口元 o の字）
  - ＋ 平常
- 表情は **3レイヤー構成**：①感情セット（口/眉/頬/涙）②目の開度（糸目/半目＝補助軸）③隈の濃さ（状態軸）
- 立ち絵は **レイヤー分離（PSD等）で納品・合成**し、組み合わせ爆発を避ける（＝発注枚数の土台）。
- **開眼（大きく目を開く）は日常表情から外す**。**正体開示・夜ケア核心の「特別カット」としてのみ残す**（最終確定は SESSION_STATE の未確定項目「開眼の扱い」で行う）。

**理由**
- 当初は「表情＝目の開度×隈のみ（喜怒哀楽ではない）」としたが、作者の作画イメージ（各感情の口/眉/頬の具体）が明確で、糸目キャラの抑制された感情表現とも両立する。
- 糸目/半目を基盤に感情を載せることで、旧却下理由「糸目キャラでは表情の幅が死ぬ」を回避しつつ喜怒哀楽を持てる。
- 開眼を"怒り"ではなく"心を許す/求める核心"に限定することで、商品の核「昼＝糸目 ↔ 夜/核心＝開眼」のギャップを一枚看板として温存できる（作者が嫌うのは「怒りの開眼」で、この作品の核心開眼は用途が別）。

**却下した案 / 変更点**
- 旧方針「表情を喜怒哀楽で切るのは却下・目の開度×隈のみ」→ **更新**。糸目/半目を基盤に喜怒哀楽をレイヤーで載せる形へ統合。
- **組み合わせをフルCGで描く**：却下。枚数が乗数的に増える。レイヤー合成にする。
- **開眼を日常表情に使う**：不使用（このキャラは本気で怒らない）。ただし核心の特別カットは残す（下記 未確定）。

**根拠・先行事例**
- 作者提示の感情別パーツ定義（上記）。
- ノベルゲーム/立ち絵差分の一般的手法（目・口・眉を別レイヤーで持ち、実行時に合成）。

**未確定（SESSION_STATE 参照）**
- **開眼の扱い**：核心の特別カットとして残す（推奨）か、完全にナシにするか。完全ナシは確定設定「夜/核心で目が開く」の撤回になる。
- **隈の濃さと数値の結線**：既存 angelFatigue に結線するか、専用 kumaLevel(0–2) を新設するか。
- **表情段階数**：目（糸目/半目〔＋開眼は上記次第〕）、隈（2段 or 3段。推奨3段）。発注枚数の確定に直結。

---

# 2026-07-13｜開眼の扱い：立ち絵から外し、夜パートの1枚絵側で表現する

**決定**
- **立ち絵の目の開度は「糸目／半目」の2値のみ**。開眼（大きく目を開く）は**表情システムに含めない**。
- **開眼は夜パート／核心の1枚絵（イベントCG）側で表現する**。
- 開眼カットの**使用回数の制限は設けない**（当初検討した「特別カット1〜2枚に絞る」案は不要と判断）。

**理由**
- 夜パートは立ち絵ではなく**1枚絵ベースで制作する方針**（作者判断）。したがって「昼＝立ち絵／夜＝1枚絵」という**メディアの切り替えそのもの**が昼夜のギャップを担保する。開眼を希少化して守る必要がない。
- 開眼が表情システムに存在しないため、**実装上のガード（ホワイトリスト等）が不要**になる。境界がメディアの違いで自然に引かれる、最も単純な構造。
- 糸目／半目の採用は作者の作画上の確定意図に基づく：①糸目キャラそのものへの嗜好 ②**半目から開けたときの色気**が見せ場である、という設計意図。

**却下した案**
- **開眼を完全にナシにする**：却下。確定設定「夜／核心で目が開く」の撤回になり、商品の核である昼夜のギャップの見せ場を失う。
- **開眼を表情enum（立ち絵の目レイヤー）に追加する**：却下。日常表情から事故で呼び出され（例：「驚き＝開眼」）、記号が摩耗する。実装をサブエージェントに委譲する際に特に危険。
- **開眼カットの使用シーンをホワイトリスト定数で管理する**：却下。夜＝1枚絵というメディア分離により不要と判明。過剰な機構。
- **開眼を正体開示シーンでも使う（2枚案）**：見送り。開示は「翼」が記号を担う。ただし1枚絵側での運用のため、厳格な禁止ではなく演出判断に委ねる。

**根拠・先行事例**
- 作者の作画意図（糸目／半目の色気）。
- ノベルゲームにおける立ち絵（反復・低コスト）とイベントCG（一回性・高コスト）の役割分担の一般則。

**波及**
- 未確定項目「開眼の扱い（U-01）」を**クローズ**。
- 「表情の段階数」の焦点が「開眼の有無」から**「半目の階調（1段 or 2段）」**へ移動。
- 新規未確定項目 **U-07「夜パートの絵の持ち方」** を追加（下記）。

**新たに顕在化した論点（未確定・SESSION_STATE 参照）**
- **U-07 夜パートの絵の持ち方**：夜を1枚絵のみで構成すると、夜のコンテンツ量がCG枚数に直結する。夜ケアはループ性があるため、CGが少ないと使い回し感が出る。将来の stage/position 軸を乗せると枚数が乗数的に増える。対案として「**夜専用立ち絵（開眼ベース・表情差分は少数）＋ 核心のCG数枚**」があり、この場合は夜のテキスト量を増やしても絵のコストが線形で済む。**発注枚数の見積もりに直撃するため、絵師探索より前に決める必要がある。**

---

# 2026-07-13｜夜ケアパートのゲーム性を確定（動詞・ポジション・おあずけ・オート）

> 仕様の詳細は「🌙 夜ケアパート仕様（v0.1）」ページに集約。本項は判断理由と却下案を記録する。
> 前提：これまで夜ケアの仕様正本はどこにも存在しなかった（CURRENT_SPEC v0.3 は全年齢前提のまま、プロトタイプは別リポジトリ）。本セッションで仕様ページを新設した。

**決定**
- **コアの動詞は「持ちこたえる」**。ゲージが自動増加し、プレイヤーは天使様に応え続けるために耐える。耐えた量＝ angelFatigue の回復量。失敗は「負け」ではなく「回復が浅い」という結果になる
- 入力は**タイミングに合わせた長押し→離すの反復**。**精度判定（PERFECT/MISS）表示なし、ゲームオーバーなし**、判定は広く取る
- 主導権の**反転なし**。シチュエーションは変わるが行為は変わらないため、**ミニゲームは1種類のみ**
- **ポジション4つ**。進行で置き換わるのではなく**選択肢として増え、以降いつでも切替可能**。**ポジション間の性能差はつけない**
- **「今夜の求め」を採用**。ポジションが2〜3に増えた段階から**時折**発生。開始前の**立ち絵会話でそれとなくヒント**。応えれば**僅かな**回復ボーナス。応えなくてもペナルティなし
- **全28夜がケア対象**。ただし**「おあずけ」択**を置く。おあずけ＝ angelFatigue が回復しない（翌日の余白が減る）が、**天使様は咎めない**
- **オートは2種に分離**。テキストのオート送り＝通常採用。**ミニゲームのオート＝採用するが、結果を「平均的な回復」に固定**する
- 夜の絵は**1枚絵ベース（U-07 案A）**。ベース4枚（ポジション別）×差分5枚。**差分は「表情バリエーション」ではなく angelFatigue と進行度に連動する状態表現**として持つ

**理由**
- 「抗う」だと、プレイヤーの手が「これは望まれざるものだ」と語ってしまい、**メカニクスが物語（＝必要とされることで、初めてここにいていいを得る）と逆を向く**。入力もゲージも体感も同一のまま、フレーミングだけで意味が反転する
- 耐えた量を angelFatigue に直結させることで、「役に立てている」がゲージで可視化され、システムが1本の線で繋がる
- ポジションが「置き換え」ではなく「累積する選択肢」であるため、**反復はプレイヤー自身の選択の結果になる**。飽きの責任が設計者側からプレイヤー側へ移る
- 毎晩必ず求められると「必要とされること」が日常化し、テーマの手触りが薄れる。**断れるからこそ、応じることが選択になる**。おあずけ択は懺悔室の3択（話す/ごまかす/黙る、すべて等価に肯定）と**同一の文法**であり、作品全体の一貫性を保つ
- 「今夜の求め」の頻度・演出・報酬を**すべて抑制**する（毎晩でない／それとなく／僅か）ことで、義務化と攻略対象化を同時に回避する
- R-18の実プレイ環境では、片手が塞がっている・集中が続かないのは**想定すべき通常状態**。ミニゲームのオートはアクセシビリティではなく**商品として必須の設計**

**却下した案**
- **「抗う」フレーミング**：却下。上記の通りメカニクスがテーマと逆行する
- **音ゲー的な精度判定（PERFECT/MISS表示）・ゲームオーバー**：却下。明確な失敗状態はこのプレイ環境ではストレスにしかならない
- **28夜のうち一部だけをケア夜にする（反復疲れ対策）**：却下。ポジションの選択制＋おあずけ択により反復がプレイヤーの選択になるため、この緩和策は不要。作者判断
- **後半で主導権を反転させ、ミニゲームを2種類にする**：却下。シチュエーションは変わるが行為は変わらないため、実装量が倍になるだけ
- **ポジションに性能差をつける**：却下。最適解が生まれ、プレイヤーが1つに固定し、他の3枚が死ぬ
- **ポジションを進行で置き換える**：却下。累積する選択肢にする
- **開眼カットの使用回数制限**：却下済み（D-14）

**設計上の要注意（アンチパターン抵触チェック）**
- CURRENT_SPEC v0.3 のアンチパターン「**痛みの数値ゲージ化とゼロ到達＝クリア**」に抵触しないこと。夜ケアで回復するのは **angelFatigue（天使様の疲労）**であって、**主人公の painType ではない**。この2つを混ぜない
- 同じく「懺悔室の性的演出への転用」も禁止のまま維持

**波及**
- **U-07（夜パートの絵の持ち方）をクローズ**：案A（1枚絵ベース）で確定
- **U-02（隈と数値の結線）の優先度が上昇**：夜CGの状態連動差分に直結するため、表情の他項目より先に決める必要がある

---

# 2026-07-13｜日数構成：28日を本編として確定（14日MVPを撤回）

**決定**
- **28日を本編とする**。v0.3 の「14日MVP ＋ 28日は拡張候補」を撤回する
- 14日目は「帰任予定日／残留選択」＝**偽りの結末**として、本編内に組み込む
- 完走する（スコープ縮小はしない）

**理由**
- 夜ケアが**商品の本体**であり、その設計（ポジション4つ・累積解禁・状態連動差分）は**28日を前提に組まれている**。14日では4ポジションを解禁しきる前に終わる
- 14日目の残留選択は「偽りの結末」として設計されており、そこで終わると**夜の関係が中途半端に切れる**
- 昼夜のギャップという商品の核を、不完全な形で出すことになる

**却下した案**
- **MVP＝14日のまま、ポジションを2〜3に減らして出す**：却下。スコープとCG枚数は抑えられるが、「28日÷4」を前提に組んだ夜ケア仕様を書き直すことになり、かつ物語が中途半端に切れる。作者判断で完走を選択

**リスク（明示）**
- スコープが実質2倍になる。個人開発の完成可能性が下がる
- v0.3 が持っていた「14日で一度着地させる」という**安全弁を失う**
- 緩和策：後半14日（15〜28日目）は重要日を絞り、通常日＋短文プールの比率を上げる。「何も起きない日」の許容という既存の設計原則がそのまま効く

**波及**
- CURRENT_SPEC を **v0.4 に全面改訂**（販売方針・日数構成・夜ケア・キャラ記号・表情システム・「今はやらないこと」を更新）
- 懺悔室イベントは 28日構成に合わせて**最低3回**（解禁時／11日目前後／残留後の再定義）へ
- 維持したもの：痛みタイプ6分類、懺悔室、ライティング規範、コマンド4種、アンチパターン群

---

# 2026-07-13｜隈と数値の結線：angelFatigue からの導出値とする（案A改）

**決定**
- **kumaLevel は状態変数として持たない。angelFatigue からの導出値（関数）とする**
- **長期の変化は「日中に溜まる疲労量そのものが減っていく」で表現する**。物語が進む＝天使様が主人公を頼れるようになる＝**疲労蓄積レートが下がる** → angelFatigue の平常値が下がる → 隈が薄くなる
- **判定タイミングを昼夜で分ける**
  - **昼の立ち絵**：その日の**朝の angelFatigue でスナップショット固定**（日中はガタつかない）
  - **夜のCG**：**リアルタイムの angelFatigue**（その夜の状態が出る）
- **例外：オープニングはシステム外**。OP の絵は導出値を使わず、演出として直接指定してよい

**理由**
- 確定設定「隈は物語の進行と、ケアの結果で増減する」により、隈は**2つの時間スケールを背負っている**。angelFatigue に素朴に直結すると隈が毎日ガタつき、「回復してきた」という長期の手応えが読めない。逆に長期だけだと、その夜のケアの結果が絵に出ない
- 疲労蓄積レートの低下で長期変化を出すと、**因果が一本で通る**：「主人公を頼れるようになったから、疲れにくくなった」。これはテーマそのもの
- 導出値にすることで**新規の状態変数がゼロ**になり、**更新ルールもゼロ**（更新忘れが構造的に起きない）
- 判定タイミングの昼夜分離により、同じ1つの数値から「昼の安定した見た目」と「夜のその場の状態」の両方を取り出せる

**却下した案**
- **専用 kumaLevel(0–2) を独立した状態変数として新設する**：却下。変数と更新ルールが1つ増える。独立変数が必要になるのは「物語イベントで隈を直接指定したい場面がある」場合だが、**作者確認の結果、そのような場面はない**（あってもOPのみ＝システム外）
- **angelFatigue に素朴に直結する（判定タイミングを分けない）**：却下。隈が日中ガタつき、長期の手応えが読めなくなる

**新たに生じるコスト（明示）**
- **疲労蓄積レート（進行に伴って低下する）**という新しい数値が必要になる。変数は増えないが、**チューニング対象が1つ増える**のは事実
- 「おあずけ」を多用すると隈が濃いまま残る。これは**正しい挙動**だが、絵として重くなりすぎないか実測が必要

**波及**
- **U-02 をクローズ**
- U-03（表情の段階数）へ進める。残る論点は **半目の階調（1段/2段）** と **隈の段階数（2段/3段。推奨3段）**
- 夜ケア仕様の「差分は状態連動」（D-15）が、これで実装レベルまで接地した

---

# 2026-07-13｜表情の段階数を確定（目3枚・隈2枚＋αクロスフェード・感情6）

**決定**
**レイヤー構成（立ち絵1ポーズあたり 計11レイヤー）**
- **目：3枚** — 糸目 / 半目 / **妖艶半目**
- **隈：2枚** — 薄 / 濃。**プログラム側のアルファ・クロスフェードで無段階に見せる**
  - 0.0〜0.5：薄レイヤーの alpha を 0→1
  - 0.5〜1.0：薄を残しつつ濃レイヤーの alpha を 0→1
- **感情セット（口・眉・頬・涙）：6枚** — 平常 / 笑 / 大笑 / 怒（頬膨らませ）/ 悲 / 驚

**妖艶半目の運用ルール**
- **表情はプリセット制にする**
  - **日常プリセット**（平常/笑/大笑/怒/悲/驚）は、目が **糸目・半目のみ**。妖艶半目を**含まない**
  - **特別プリセット**にのみ妖艶半目を含め、**シーン側で明示的に指定したときだけ呼べる**
- **使用の判断基準（1行）**：**天使様の側が「主人公を必要としている」ことが漏れる瞬間にだけ使う**。時間帯ではなく意味で縛る
- **実運用は物語終盤が中心**。昼パートでの使用は全28日で**3〜5回程度**を上限の目安とする

**発注書への必須記載**
- 隈は **2枚を重ねて使う前提**であること
- 隈レイヤーの**合成モード（通常／乗算）**の指定
- 妖艶半目の**使用場面**（特別プリセット限定・終盤中心）

**理由**
- **隈5段の決め打ちを却下した理由**：濃い隈は「薄い隈を濃くしたもの」ではない。実際の絵では色味（赤み・紫み）・形の広がり・目元のくぼみが変わる。単一レイヤーの alpha を上げるだけでは、のっぺり黒ずむだけで「限界まで疲れた天使様」の説得力が出ない
- **2枚＋クロスフェードなら、5枚より少ない発注で、見た目は無段階になる**。段階数の議論そのものが消える
- 目が **糸目 → 半目 → 妖艶半目 →（夜のCGで）開眼** の4段グラデーションになり、記号が階段状に並ぶ。開眼を立ち絵から外した判断（D-14）と噛み合う
- **プリセット制にする理由**：妖艶半目を単なる選択肢として置くと、実装時（特にサブエージェント委譲時）に日常シーンから乱発され、記号が摩耗する。D-14 で開眼を表情enumから外したのと同じ構造の事故
- **意味で縛る理由**：時間帯（昼/夜）で縛ると、昼に1回出す自由を失う。「必要としていることが漏れる瞬間」で縛れば、作品の核（必要とされることで居場所を得る）と一直線に繋がり、昼に1回出るだけでも刺さる

**却下した案**
- **隈を5段（5枚）決め打ちで発注する**：却下。枚数が増えるうえ、離散段階のままになる。2枚＋クロスフェードの方が安く滑らか
- **隈を1枚だけ描き、alpha だけで濃さを出す**：却下。色味・形の変化が出ず、濃い隈の説得力が失われる
- **妖艶半目を昼で全面的に使う／全く使わない、のいずれかに固定する**：却下。「ここぞで使う」選択肢を残したいという作者意向。プリセット制＋意味による判断基準で、自由と事故防止を両立させた
- **妖艶半目を通常の表情enumに含める**：却下。日常シーンから事故で呼ばれる

**波及**
- **U-03 をクローズ**。表情システムは確定
- 発注枚数の見積もりが可能になった → イラストレーター探索フェーズへ
- 未確定として残るのは **髪色・差し色（U-04）** と、数値系（疲労蓄積レート、ゲージ速度・判定幅、ポジション解禁条件、おあずけ回復量、「今夜の求め」頻度）

---

# 2026-07-13｜疲労蓄積レートは「28日シミュレーション」で先に当たりをつける

**決定**
- 疲労蓄積レートの低下幅は**机上で決めない**。ただし**本文の完成を待つ必要もない**
- **数値だけを28日ぶん回すシミュレーションスクリプト**を先に作る（本文なし。ケア成功率を振って、angelFatigue と隈の推移をプロットする）
- これを次タスクとする

**理由**
- 「28日通してみないと分からない」は正しいが、28日分の本文を書き終えるまで判断を保留すると、**手戻りが最大化するタイミングで問題が発覚する**
- 数値の挙動だけなら本文と独立して検証できる。「30日目にようやく隈が薄くなる」といった事故を、実装コストがほぼゼロの段階で潰せる
- 隈を導出値にした（D-17）ため、**隈の見え方＝ angelFatigue の推移そのもの**。数値が決まれば絵の見え方も決まる

**検証項目**
- ケア成功率（低／中／高）× おあずけ頻度（低／中／高）のマトリクスで、28日を通した angelFatigue の推移
- 隈が「薄くなってきた」と体感できるのが何日目か
- 終盤に隈が完全に消えてしまわないか（消えると「天使様が回復しきった」＝物語が終わってしまう）

---

# 2026-07-13｜夜ケアの動機は前半後半で入れ替わる（後半に「関係の深まりを示す値」を置く）

**決定**
- **天使様の疲労は 14日目前後で概ね取り除ける**設計とする。angelFatigue は前半で下がりきってよい
- **夜ケアの動機を前半後半で入れ替える**
  - **前半（1〜14日）**：天使様を回復させるためにやる。**目的のある行為**。結果は **angelFatigue の回復**に乗る
  - **後半（15〜28日）**：回復させる必要はもうない。それでもやる。**行為自体が目的**。結果は**別の値（関係の深まりを示す値）**に乗る
- **後半用の値を1つ置く**ことを確定する（**何を表す値にするかは次セッションで決定**）
- 転換を**明示する**：疲労が0になった夜、天使様の台詞が変わる。「もう、疲れてはいないのですけれど」——それでも求めてくる。**メカニクスの転換を、物語の転換として見せる**

**理由**
- 14日目は「帰任か残留か」を選ぶ日（＝偽りの結末）。**疲労が取れきった地点で残留を選ぶ**ことになるため、「もう回復させたのだから、あなたはここにいなくていい」という状態で、それでも残る、という構造が成立する
- これにより **「必要とされること」→「一緒にいたいこと」への転換**が、日数構成と隈のカーブで同時に起きる。作品の核（主人公は必要とされることで居場所を得る）を、その先へ進める
- **後半に値を置かないと後半が空洞になる**：angelFatigue が0で張り付くと、持ちこたえてもおあずけしても、絵も数値も変わらない。これは「ケア自体が目的」ではなく単なるご褒美モードであり、関係が最も深まるはずの後半14夜が惰性になる

**却下した案**
- **後半は数値が動かない「純粋な描写の場」として設計する（テキスト量で持たせる）**：却下。成立はするが、後半14夜の手応えがすべて本文任せになり、負荷とリスクが高い

**次セッションへの制約条件（重要）**
- **新しい値は「絵に出る値」でなければならない**。理由：後半は angelFatigue が0で張り付くため隈が動かない。夜CGの差分5枚をすべて fatigue に連動させていると、**後半の絵が固まる**
- **差分の駆動軸を分担する（発注枚数は増えない）**
  - **隈** ← angelFatigue（前半で主に動く）
  - **表情・汗など残りの差分** ← **新しい値**（後半で主に動く）

**明示しておくコスト（作者了承済み）**
- 増えるのは**量ではなく難易度**。後半14夜は「**疲れていないのに求める天使様**」を書き切る必要がある。前半は求める理由を「疲労」に逃がせるが、後半は逃げ場がない。ここが甘いと惰性の反復になる
- 状態変数が1つ増える（更新ルールも1つ増える）

**波及**
- **次タスク：後半用の値の定義**（何を表すか／絵にどう出るか／更新ルール）
- 28日シミュレーションの検証項目を修正：「終盤に隈が完全に消えないか」は**懸念ではなくなった**。代わりに「**14日目前後で概ね消えるか**」を確認する
- 夜ケア仕様（D-15）の「差分は状態連動」を、**前半＝fatigue軸／後半＝新値軸**へ分割する必要がある

---

# 2026-07-15｜Notionページの運用ルール（恒久／使い捨ての二分と、削除基準）

**決定**
- **チャット側と Claude Code 側のやり取りは、Notion を経由して行う**（コピペ事故とチャット履歴消失を回避するため）。この方針は `.claude` 側にも記載済み
- **Notionページを2種類に分け、寿命を変える**

| 種別 | 対象 | 扱い |
| --- | --- | --- |
| **恒久ページ** | CURRENT_SPEC / 夜ケアパート仕様 / DECISION_LOG / SESSION_STATE | **上書き更新**していく。消さない。積み上がらない |
| **使い捨てページ** | タスク指示文、棚卸し・調査結果などの一時的な成果物 | 役目を終えたら**削除**する |

- **使い捨てページには命名プレフィックスを付ける**：`TASK：` / `WIP：` 等。機械的に判別できるようにする
- **削除の基準（1行）**：**そのページの結論が DECISION_LOG または CURRENT_SPEC に転記されたら、元ページは消してよい**

**理由**
- 判断（なぜそう決めたか）が残っていれば、その材料（生データ・調査結果・指示文）は捨てられる。これは本プロジェクトのドキュメント方針そのもの
- Notion経由にすると成果物が積み上がるため、削除の基準を先に決めておかないと、後で必ず「どれが生きているか分からない」状態になる

**却下した案**
- **使い捨てページを「圧縮」して残す**：却下。中途半端に要約すると、原本でも要約でもないものが残り、次に読んだときにどちらを信じるべきか分からなくなる。**残すか、消すか の二択にする**

**運用メモ**
- 現在の使い捨てページ：`📋 TASK：状態棚卸し（Claude Code 指示文）`
- 戻ってきた `docs/STATE_INVENTORY.md` も使い捨て扱い。そこから決めたことを DECISION_LOG に転記した時点で、指示文・棚卸し結果の両方を削除してよい

---

# 2026-07-15｜状態棚卸しの結果を受け、作業順序を「土台優先」に組み替える

**棚卸しで判明した事実**（詳細は「📦 STATE_INVENTORY」）
1. **`painPoints` / `painType` / `painSeeds` はコードに0件**。あるのは `pain_tired` / `hide_pain` の boolean 2つだけ。**6分類は一度も実装されていない**
2. **`angelFatigue` が単調減少する。増加させる処理がコードに存在しない**（chores と夜ケアで減るのみ）
3. **ドキュメント間で不一致**：夜ケアの数値仕様（careStage 1〜5 等）を `NEXT_TASKS.md` が「確定済み」と記述しているが、実装には無く、`CURRENT_SPEC.md` は「未実装」と書いている
4. `content-config.js` の既定が `all_ages`。R-18のみ方針と逆（切替1箇所）
5. `careStyle` / `angel_asked_for_care` / `mentalMarginMax` が定義のみで未使用
6. `angelExpression` は自由記述文字列で、値の集合が固定化されていない（tired / soft / gentle / warm / sleepy / shocked / surprised / sad / concerned / normal 等が散在）

**決定：作業順序**
痛みタイプの再設計より先に、**土台を直す**。
1. **`angelFatigue` の加算処理を設計・実装する**（例：夕方の祈り＝天使様のお仕事で疲労が溜まる）
2. **ドキュメント間の矛盾を解消する**（`NEXT_TASKS.md` の「確定済み」記述を DECISION_LOG / CURRENT_SPEC に合わせる）
3. **`content-config.js` の既定を `r18` にする**
4. **その後**、痛みタイプ6分類の再設計 → 28日シミュレーション

**理由**
- **疲労の加算が存在しない状態では、何も検証できない**。隈の導出（D-17）、疲労蓄積レートの低下、14日での疲労回復（D-20）、後半の動機転換——これらはすべて「昼に溜まり、夜に減る」往復を前提にしている。加算がなければ 28日シミュレーションを回しても数字が意味を持たない
- ドキュメント間の矛盾は放置すると事故る。**DECISION_LOG が正**であることを、他ドキュメントにも徹底させる必要がある
- 痛みタイプは**まだ一度も実装されていない**ため、急ぐ必要がない。むしろ「捨てるものが何もない」＝**実装前に直せる最良のタイミング**であり、土台を固めてから落ち着いてやればよい

**却下した案**
- **痛みタイプの再設計を先にやる**：却下。土台（疲労の加算）が無い状態で痛みの構造だけ精緻化しても、検証できず、手戻りのリスクが残る

**波及**
- 28日シミュレーション（D-19）は、**`angelFatigue` の加算処理が入ってから**でないと実施できない。前提条件として明記する
- 「疲労蓄積レートの低下幅」を未確定項目として立てていたが、**そもそも加算そのものが未実装**だった。レート以前の問題として扱う
- `angelExpression` の値の集合が固定化されていないため、新表情システム（D-18：目3・隈2・感情6）への移行時に**棚卸しとマッピングが必要**。実装フェーズの前提として記録する

**次のチャットセッションで決めること**
- **`angelFatigue` の加算設計**：いつ溜まるか（夕方の祈り？ 日単位？）／どれだけ溜まるか／進行に伴って加算量が減っていく（＝疲労蓄積レートの低下、D-17）仕組みをどう組むか

---

# 2026-07-15｜angelFatigue の加算設計（疲労はどこから溜まるか）

**背景**
状態棚卸しの結果、**`angelFatigue` に加算処理が存在しない**（単調減少）ことが判明した。「昼に溜まり、夜に減る」往復が実装されておらず、隈の導出（D-17）も、14日での疲労回復（D-20）も、28日シミュレーション（D-19）も、この上に成り立っている。本項でその土台を定める。

**決定**

### 加算（疲労が溜まる）

| タイミング | 量 | 意味 |
| --- | --- | --- |
| **夕方の祈り**（天使様の日課） | **大きく**溜まる | メインの加算源。天使様のお仕事。プレイヤーの行動とは無関係に毎日発生する |
| **昼、主人公が「休む（rest）」** | **少し**溜まる | 主人公が休んでいる間、天使様が家事をするため |

### 減算（疲労が減る）
- **昼、主人公が「家事（chores）」** → 天使様の負担が減る（既存実装：−1）
- **夕方、主人公が「祈る（pray）」** → **調律が上がる ＋ ほんの少し疲労の上昇を抑える**（既存の pray コマンドに役割を追加。新要素はゼロ）
- **夜ケア** → 減る（本筋。ミニゲームの結果で回復量が変わる）

### 信頼による低下（＝疲労蓄積レートの低下）
- **`trust` が上がると、疲労の蓄積量が減っていく**
- **かかり方：加算量そのものに全体で効く**（祈りのみに限定しない）
- これが D-17 で言う「疲労蓄積レートの低下」の実体。**新しい状態変数を作らず、既存の `trust` で表現できる**

### スケール
- 現在の数値は **14日構成を前提とした上限**になっているため、**一旦すべて2倍**にする（`angelFatigue` の範囲 0–10 → **0–20** を含む）
- 最終的な数値は **28日シミュレーションで検証して決める**。机上で確定しない

### 上限到達時の扱い
- **描写のみ。ペナルティは科さない**
- 上限に張り付いている間、**天使様の様子が変わる**（隈が最大／台詞が短くなる／反応が薄くなる）
- **`trust` は下げない。ただ、上がらない**
- プレイヤーは「何かがうまくいっていない」ことに、**絵と台詞で気づく**

**理由**
- 「主人公が休むと天使様が疲れる」構造により、**プレイヤーは毎日「自分を休ませるか、天使様を休ませるか」を選ぶ**ことになる。「休んでいい」というテーマに、メカニクスで問いを立てられる
- 夕方の祈りは天使様の使命であり、プレイヤーには止められない。**だが「祈る」コマンドを選べば加算を抑えられる**ため、プレイヤーの選択はちゃんと効く。加えて夕方の疲労を夜ケアで取るのが本筋であり、回復量はミニゲームの結果で変わるため、プレイヤーの関与は十分にある
- `trust` による低下は「頼れるようになったから疲れにくくなった」を既存変数で表現できる。**新変数ゼロ**

**却下した案**
- **上限到達で `trust` を下げる**：却下。**デススパイラルを生む**。trust低下 → 疲労蓄積が増える（trust は加算量全体に効くため）→ また上限に達する → trust がさらに下がる、という抜け出せない構造になる。しかも trust は夜ケアで +1 ずつしか増えず、回復が遅い
  - さらに本質的な問題として、**これは「罰」である**。本作は「失敗を作らない」で一貫している（おあずけを咎めない／懺悔室の3択はすべて等価に肯定／ミニゲームに失敗判定なし）。ここで初めてペナルティを導入すべきではない
  - 「やるべきことをやれていない」を伝えたいという意図は正しいが、**罰ではなく描写で出せる**。数字で殴らなくても伝わるし、その方が本作の文法に合う
- **上限到達で `mentalMargin` の回復量を下げる**：見送り。デススパイラルにはならない（mentalMargin は rest で戻せる）ため trust低下案より安全だが、描写のみで足りると判断
- **信頼値が低い状態で「話しかける」と疲労が溜まる**：却下（作者判断で撤回）
- **`trust` による低下を「祈りの加算」にだけ効かせる**：却下。全体に効かせるほうがシンプル。バランス調整の難しさはシミュレーションで潰す

**波及**
- **28日シミュレーション（D-19）の前提が揃った。** 実施可能になる
- **シミュレーションで最初に見るべき比率**：夕方の祈りの加算量 と、rest/chores/pray による増減の比率。祈りが大きすぎるとプレイヤーの行動が無意味になり、小さすぎると天使様の孤独が伝わらない
- 上限到達時の「様子が変わる」描写は、**表情システムと台詞の両方に影響する**。実装時に定義が必要

**未確定（シミュレーションで決める）**
- 各加算・減算の具体的な数値
- `trust` による低下の効き方（線形か／段階か／効きすぎないか）
- 上限（0–20 が妥当か）

---

# 2026-07-14｜angelFatigue の加算設計を実装（往復させる土台）

**決定**
`angelFatigue` に、これまで無かった「加算」を追加した。あわせて stats 全体を28日想定の2倍スケールに変更した。
- **夕方の祈り（天使様の日課）**：プレイヤーの行動と無関係に、夕方フェーズに入った瞬間（`advanceTime()` 内、timeSlot が `'evening'` になる唯一の1箇所）に毎日1回だけ大きく加算（既定 +4）。二重加算を避けるため呼び出し箇所は1つに限定。
- **昼、主人公が rest**：天使様が代わりに家事をする分、少量加算（既定 +1）。夕方の rest は対象外（夕方は天使様が祈りの務め中で家事を肩代わりする状況ではないため）。
- **trust による加算量の低下**：新しい状態変数は作らず、既存 `trust` を使って `fatigueGainAfterTrust()` で加算量を `max(0, 1 − trust×trustReductionRate)` 倍に減衰。減算（chores/夜ケア/夕方祈るの相殺分）には適用しない。
- **夕方の祈る（pray）**：既存の prayerTuning 上昇に加え、日課の加算をほんの少し相殺（既定 −1）。
- 増減の数値は `app.js` の `STAT_CONFIG` / `FATIGUE_CONFIG` に集約。以後の数値調整はここだけ触ればよい。
- **上限到達時は clamp で止めるだけ。ペナルティは科さない**（trust を下げない／mentalMargin 回復量を下げない／描写変更は別タスク）。
- stats 全体（trust/prayerTuning/caretakerAptitude/mentalMargin/mentalMarginMax/angelFatigue）の初期値・範囲・増減幅・比較閾値を2倍化。`scenario-data.js` の `statusTexts` 表示閾値も追随して2倍化。

**理由**
- `angelFatigue` がこれまで単調減少だったため（STATE_INVENTORY §8）、「昼に溜まり夜ケアで減る」本来の往復設計が成立していなかった。今回その土台を入れた。
- 増減を1箇所の設定オブジェクトに集約したのは、この後の28日シミュレーションでの数値調整を差分最小で行えるようにするため。

**却下した案（再提案しない）**
- **上限到達時に trust を下げる**：却下。デススパイラルになるため。
- **上限到達時に mentalMargin の回復量を下げる**：却下（見送り済み）。
- **専用の新しい状態変数を新設する**：却下。既存 `trust` で表現する方針が確定済み。
- **夜ケアにミニゲーム（careEnergy/careStage/stageProgress）を実装**：今回のスコープ外（既存の固定処理を維持）。

**根拠**
- タスク指示（確定済み仕様）。動作確認：Playwright の1〜7日通しプレイで、angelFatigue が往復（rest-only は上限付近、chores-only は下限付近）することを確認。pageerror 0、`node --check` 通過。

**仮置き数値（28日シミュレーション調整用・`app.js` に集約）**
- `FATIGUE_CONFIG`: eveningPrayerGain=4 / restGain=1 / eveningPrayReduce=1 / trustReductionRate=0.01
- `STAT_CONFIG`: chores{caretaker+4, fatigue−2, margin−2} / pray{tuning+4(+2 if fatigue≤6), margin−2} / rest{margin+4} / talk{trust+4} / eveningChores{caretaker+2, margin−2} / eveningPray{tuning+6(+2), margin−2} / sleep{margin+4} / nightCare{trust+2, fatigue−2(+追加−2 if margin≥14), margin−2}
- stats 範囲: trust/prayerTuning/caretakerAptitude 0–60、angelFatigue 0–20、mentalMargin/Max 0–20

---

# 2026-07-15｜ドキュメントの正本を Notion に反転する

**背景（Claude Code からの申し送り）**
土台①（angelFatigue の加算処理）の実装完了時、以下が報告された：
- Notion の SESSION_STATE / DECISION_LOG は、**チャット側で大幅に加筆されている**（D-16〜D-20 / U-07クローズ / 表情11レイヤー / リスク表 等）
- 一方、**ローカルの `docs/*.md` はそれらを含まない旧版**のまま
- つまり「ローカルが正、Notionはミラー」という当初の原則と、**実態が逆転している**

**決定**
- **Notion を正本とする。** ローカルの `docs/*.md` は**ミラー**とする
- Claude Code はタスク完了時、**Notion → ローカル**の方向で同期する
- SESSION_STATE 冒頭の「ローカルの `docs/SESSION_STATE.md` が正、このページはミラー」という記述を**逆に修正する**
- **次のタスクは「docs 同期」**。土台②③（ドキュメント矛盾解消＋content-config の r18 化）**より先**に実施する

**理由**
- 実態として、**設計判断はすべてチャット側 → Notion に書かれている**。Claude Code も Notion を読んで実装している。Notion が実質的な正本になっている
- ローカル `docs/*.md` は「チャット履歴が失われてもリポジトリに設計が残るように」という目的で維持する。**保険としての役割は変わらない**が、**更新の起点ではない**
- **順序の理由**：土台②は「ドキュメントの矛盾解消」タスクである。**矛盾したままの旧版 docs に対して矛盾解消を行うと、古い内容に合わせてしまう**。同期が先でなければならない

**却下した案**
- **ローカルを正本のまま維持し、チャット側がローカルに書けるようにする**：却下。チャット側（Claude）はリポジトリに直接書き込めない。実行不可能

---

# 2026-07-15｜土台①の実装内容を確認・承認

**Claude Code からの要確認2点に対する回答**
1. **夕方の `rest` を加算対象外にした** → **承認**
   - 「昼、主人公が休む → 天使様が家事をする → 疲労が溜まる」という因果であり、**夕方は天使様が祈りの務めに入っている＝家事をしていない**。したがって夕方に休んでも天使様は疲れない
   - literal解釈ではなく、**設計意図として正しい**
2. **`scenario-data.js` の `statusTexts` 閾値も2倍化した** → **承認**
   - 「数値を全部2倍」の指示に閾値が含まれるのは当然。2倍化しなければ状態文の表示が壊れる。**やらなかった方が問題だった**。スコープ内と判断する

**実装された内容（確認済み）**
- `angelFatigue` に加算を追加：**夕方の祈り＝日課で毎日1回のみ・大**／**昼の `rest`＝少**
- `trust` で蓄積量を低減
- 上限は **clamp のみ。ペナルティなし**（trust 減少なし）
- **新 state 変数なし**、`END_DAY=7` 維持、ミニゲーム不実装
- 増減定数を `STAT_CONFIG.FATIGUE_CONFIG` に集約、stats 全体を2倍スケール化
- 独立検証：10→11→16、夜ケアで12％降下→再上昇18 と往復を確認。rest-only ↔ chores-only で推移が明確に相違

---

# 2026-07-15｜疲労値の確定（最終・A＋B＋D改）

**決定（採用値・app.js 反映済み）**
28日シミュレーション（3回）を経て、`angelFatigue` の数値を確定した。
- **A**：`statsDefault.angelFatigue = 20`（初期＝範囲0–20の上限。疲れきった天使様）
- **B**：`FATIGUE_CONFIG.eveningPrayerGain = 6`（4→6。日課の再蓄積を強め夜ケアの純減を薄める）
- **D改**：`FATIGUE_CONFIG.trustReductionRate = 0.009`（0.01→0.009。後半まで蓄積レートを緩やかに下げる。**trust の伸び自体＝`talk.trust`/`nightCare.trust` は不変**＝信頼パラメータ全体への波及を避ける）
- **C（夜ケア回復量）・範囲（0–20）・式は不変更。**

**理由（1行）**
標準プレイ（balanced）で `angelFatigue` がちょうど14日目に概ね取れ（≤2）、28日目まで0で安定するため（D-20「14日で疲労が取れ、後半は bond にバトンタッチ」に一致）。

**既知の特性（報告）**
着地日は昼の行動に依存：世話中心=11日／balanced=14日／休みがち=16日／関係中心（家事を一切しない極端プレイ）のみ頭打ち（＝「世話をしなければ疲労は取れない」としてテーマ上許容）。最終値は将来の夜ケア成功度・実プレイで再調整の余地あり。検証: `node tools/sim-fatigue-28day.mjs` / `tools/sim-fatigue-final.mjs`。
