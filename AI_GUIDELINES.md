# AI_GUIDELINES.md

**プロジェクト名（仮）:** 辺境教会の天使様と、はじまりの生活  
**文書の目的:** Cursor / Claude / Codex に実装・仕様整理を依頼するときの **共通ルール**  
**最終更新:** 2026-07-01  
**関連文書:** `CURRENT_SPEC.md`（仕様の正本）

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| ジャンル | DLsite 向け **同棲生活シミュレーション** |
| 舞台 | 地方（辺境）の小さな **木造教会** |
| 主人公 | 大教会から **14 日間** 短期任務で派遣された人物。先任シスターの世話・雑務を担う |
| ヒロイン | **シスター 1 名**（実際は上位存在／天使的存在）。最初から派手に説明しない |
| 進行 | **朝 / 昼 / 夜** の時間帯進行。日常・会話・祈り・休息を重ねる |
| 主題 | 癒し、生活感、見守られる感覚、やさしい反応 |
| 現フェーズ | **全年齢基盤** を先に作る。まず **2〜3 時間遊べる小さな基盤** |
| 実装 | 外部ライブラリなし。`index.html` / `style.css` / `app.js` の 3 ファイル |

### AI への依頼の基本形

```
CURRENT_SPEC.md と AI_GUIDELINES.md に従って、
[具体的に1つだけ] を実装してください。
```

仕様の詳細は **CURRENT_SPEC.md** を参照すること。本ファイルは **作り方・守り方** のルール集。

---

## 2. 最重要ルール

依頼内容と矛盾する場合、**本セクションを最優先** する。

| # | ルール |
|---|---|
| 1 | **1 回 1 機能** — 1 依頼でやることは 1 つだけ |
| 2 | **最小差分** — 目的達成に必要な行だけ変更する |
| 3 | **大規模リファクタリング禁止** — 構造の作り直し・全面書き換えをしない |
| 4 | **既存構造をなるべく維持** — `sceneDefinitions` / `freeActionEvents` / `forcedNightEvents` 等のデータ駆動パターンを踏襲 |
| 5 | **まず全年齢基盤** — 成人向け要素は入れない |
| 6 | **成人要素は後から別レイヤー** — 将来用の「枠」言及のみ可。具体化・実装は禁止 |
| 7 | **複数ヒロイン化禁止** |
| 8 | **コマンドを増やしすぎない** — 現状 4 種（家事 / 祈る / 休む / 話しかける）を基準 |
| 9 | **会話分岐を増やしすぎない** — フラグ 1〜2 個で足りる設計 |
| 10 | **大規模な好感度システムにしない** — `relationStage` の単純加算で足りる |
| 11 | **朝/昼/夜の基本導線を壊さない** — 行動 → 結果 → 時間進行 →（夜は強制イベント） |
| 12 | **1 画面 UI を維持** — 別ページ・複雑な画面遷移を増やさない |
| 13 | **Live2D / ボイス / 外注素材は後回し** — プレースホルダーとテキストで十分 |
| 14 | **README 更新不要** — 明示されない限り README は触らない |
| 15 | **Shell 実行不要** — 依頼に含まれない限りターミナルコマンドを走らせない |
| 16 | **関係ない UI 変更禁止** — 依頼外のレイアウト・配色・コンポーネント変更をしない |
| 17 | **変更したファイルと確認手順を必ず提示** — 作業完了時の必須出力（§11 参照） |

---

## 3. MVP 範囲

### やること（現基盤）

- **1 ヒロイン**（シスター／天使様）
- **1 画面 UI**（タイトル / 本編 / 終了は同一 HTML 内）
- **朝・昼・夜** の 3 時間帯
- **少数コマンド**（4 種）
- **データ駆動** の会話・イベント追加
- **localStorage** セーブ / ロード
- **3 日分** の企画検証モック（→ 将来 14 日へ拡張）

### やらないこと（MVP 外）

- 複数 END・複数ルートの本格実装
- ギャラリー、実績、探索、戦闘
- サーバーセーブ、多言語、Steam 版
- スマホ最適化

詳細なイベント一覧・フラグ定義は **CURRENT_SPEC.md** を正とする。

---

## 4. 守るべきゲーム体験

実装・文案追加の判断基準。**機能より先にここを満たす**。

| 体験 | 具体 |
|---|---|
| **癒し** | 急がせない。責めない。静かな余白 |
| **生活感** | 掃除、食事、祈り、寝る。地味な積み重ね |
| **反応の気持ちよさ** | 行動に対してシスターが **短く具体的** に応える |
| **短い callback** | 前日の選択・会話を **1 文で拾う**（長い説明にしない） |
| **会話の繰り返し感を減らす** | day × timeSlot × action で差をつける。fallback は最後の手段 |
| **受け止められている感じ** | プレイヤーの弱さを断定せず、そっと見守る |
| **神秘性はにじませる** | 設定説明・世界観講義を避ける。日常の隙間に翼・疲れ・古さを置く |

### シスター（天使様）の描き方

- 最初から「実は天使です」と長々説明 **しない**（開示タイミングは spec 準拠）
- 事務的だが慈悲深い。距離感は **少しずつ** 縮む
- カウンセラーでも説教者でもない

### テキスト NG 例

- プレイヤーの感情・痛みを勝手に断定する
- 説教・説明会話が続く
- 同じ文言のコピペが連続する（fallback 乱用）

---

## 5. 実装時の注意

### ファイル役割

| ファイル | 触る場面 |
|---|---|
| `app.js` | 会話・イベント・ロジック追加（**主にここ**） |
| `index.html` | UI 要素の追加が **どうしても必要** なときだけ |
| `style.css` | 上記に伴う最小限の見た目調整のみ |

### データ追加の定番場所（app.js）

```
sceneDefinitions      … 1 日目固定シーン
slotIntroTexts        … 自由行動の時間帯導入
freeActionEvents      … 朝/昼の行動反応
forcedNightEvents     … 夜の強制イベント
actionDefinitions     … コマンドボタン（増やす場合は spec 更新後）
memoryFlagsDefault    … フラグ初期値
```

### コーディング方針

- 既存の命名・インデント・データ形状に合わせる
- 1 イベント = 1 オブジェクト。ロジックを HTML に書かない
- 条件分岐は `condition: (state) => boolean` で書く
- 不要な抽象化・ヘルパー乱立・コメント過多を避ける
- **動く最小実装** を優先。きれいさより差分の小ささ

### 時間進行（壊してはいけない導線）

```
[自由行動 select] → 行動選択 → 結果表示 →「続ける」
  → 時間進行（朝→昼→夜→翌朝）
  → 夜: forcedNightEvents があれば自動表示
  → 選択 or 続ける → 翌時間帯 or 終了
```

オープニング（`phase: 'opening'`）中に自由行動 UI を出さない。

---

## 6. 会話・反応ログの方針

### 会話を足すとき

1. **CURRENT_SPEC.md** の範囲内か確認
2. 既存イベントと **被らない id** を付ける
3. `day` / `timeSlot` / `action`（または `scene` 連鎖）を明示
4. 必要なら `setFlags` / `relationChange` を **最小限**
5. fallback 頼みにせず、**その時間帯専用の 1 本** を書く

### ログ（`addLog`）

- 短い要約（1 行）。プレイヤー向け履歴として読める長さ
- ネタバレ過多・開発者向け内部名は書かない

### callback の書き方

- **拾う → 短い反応 → 距離感 +1 程度** で終わらせる
- 2 日目の選択を 3 日目夜に参照、など **1 フック 1 回** が理想
- 新 callback を足すときは、対応する `memoryFlags` を spec と同期

---

## 7. 成人要素の扱い

| 項目 | 方針 |
|---|---|
| 今回 | **具体化しない・実装しない・文案も書かない** |
| 優先 | 全年齢の生活シミュレーションとして **まず成立** させる |
| 将来 | 親密イベント枠は **保留** として扱う（コメント 1 行程度の言及のみ可） |
| AI 禁止 | 依頼されていない **成人向け描写・シーン・選択肢の勝手追加** |

「後から別レイヤー」とは、**別データ・別フラグ・別ビルド** を想定した設計を指す。現行 `app.js` に R18 分岐を混ぜない。

---

## 8. 今はやらないこと

依頼に含まれていても、**単独では着手しない**（明示指示があれば spec 更新後に検討）。

- 実装以外の文書更新（`CURRENT_SPEC.md` / `DATA_MODEL.md` / `NEXT_TASKS.md`）— **依頼された文書だけ**
- 成人要素の具体化
- 複数ヒロイン化
- Live2D・ボイス・BGM/SE 本番素材・背景・立ち絵外注
- 大規模 UI 変更・デザイン刷新
- 大規模リファクタ・フレームワーク導入
- Shell 実行・README 更新（§2 参照）
- 関係ないファイルの整形・lint 全ファイル適用

---

## 9. すぐ直すべきバグ

以下は **最優先で修正** する。新機能より先。

| 症状 | 目安 |
|---|---|
| 画面が真っ白 | JS エラー、DOM 参照ミス、初期化失敗 |
| 日付 / 時間帯が進まない | `advanceTime` / `freeStep` 遷移の不整合 |
| コマンドが押せない | `disabled` 条件、`phase` / `freeStep` の取り違え |
| 会話が表示されない | シーン id 不一致、空テキスト、render 分岐漏れ |
| 状態保存が壊れる | `localStorage` キー、JSON 復元、`memoryFlags` マージ |
| 同じ会話だけが極端に連続 | fallback だけが選ばれ続ける選出バグ |
| 休む / 次時間帯へ進む基本導線が壊れる | 選択肢・「続ける」・夜イベント後の遷移 |

修正時も **§2 最小差分** を守る。ついでにリファクタしない。

---

## 10. 後回しでよいもの

バグではない。依頼がなくても **触らなくてよい**。

- 文言の細かい好み（トーンは spec 準拠なら OK ライン）
- 表情差分・立ち絵・背景素材
- ボイス・Live2D
- 成人要素
- 複雑な分岐・ルート分岐
- エンディング大量追加
- 演出強化（アニメーション、パーティクル、画面シェイク）
- パフォーマンス最適化（現規模では不要）
- 未接続フラグの callback 実装（spec の保留項目）

---

## 11. 作業後に必ず提示するもの

**すべての実装依頼** の完了報告に、次を含める。

### 1. 変更したファイル

```
変更:
- app.js（4日目朝の freeActionEvents を追加）
```

触っていないファイルは列挙しない。触ったら **必ず全部** 書く。

### 2. 変更内容の要約（1〜3 文）

何を・なぜ・プレイ上どう変わるか。

### 3. 確認手順

ブラウザで `index.html` を開き、**再現手順を番号付き** で書く。

```
確認手順:
1. index.html をブラウザで開く
2.「はじめから」→ … → 4日目朝まで進める
3.「家事をする」を選び、追加した会話が出ること
4. セーブ → リロード → ロードで状態が復元されること
```

Shell は使わない。自動テストが無い前提。

### 4. スコープ外にしたこと（任意・短く）

依頼に含まれていたが今回やらなかったことがあれば 1 行。

### 完了報告の NG 例

- ファイル名だけ並べて終わる
- 確認手順が「動作確認してください」だけ
- 依頼外のリファクタに触れたのに記載しない

---

## クイックチェックリスト（作業前）

- [ ] 依頼は **1 機能** に絞れているか
- [ ] CURRENT_SPEC.md と矛盾しないか
- [ ] 全年齢のみか（成人描写を足していないか）
- [ ] コマンド・分岐・ヒロインを増やしていないか
- [ ] 朝/昼/夜導線・1 画面 UI を維持しているか
- [ ] 差分は最小か（関係ない UI / README / Shell なし）

## クイックチェックリスト（作業後）

- [ ] 真っ白・進行不能・セーブ壊れがないか
- [ ] 変更ファイル一覧を出したか
- [ ] 確認手順を具体的に書いたか

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-07-01 | 初版作成 |
